# junominu.com 작업 로그 (WORKLOG)

> 세션별 **작업 내역 · 현재 상태 · 다음 할 일 · 핵심 참조**를 기록한다.
> 돌아와서 이어 작업할 때 **이 파일부터** 읽으면 어디까지 했는지 바로 잡힌다.
> (사이트 사용법·구조는 `CLAUDE.md`)
>
> ⚠️ **2026-08-19: 스레드 스킬 2종(`threads-post`·`threads-reply`)을 삭제했다**(사용자 지시).
> 아래 07-22~08-05 기록에 나오는 `threads-log.md`·`threads-strategy.md`·`voice-profile.md`·`drafts/` 는 **더 이상 없다.**
> 그 기록들은 "그때 이랬다"는 이력이라 그대로 두지만, **파일을 찾으러 가지 말 것.** 필요하면 커밋 `de0a05e` 에서 꺼낸다.

---

## ▶ 지금 할 차례 (2026-08-20 기준)

> 🔴 **이 블로그의 목적은 「제품 홍보」다**(08-20 확정). 예전 스킬이 *"제품 자랑이 아니라 만든 과정의
> 기록"* 이라고 써 뒀는데 **틀린 전제였다** — 제작기는 수단이다. 모든 글은 **「무엇을 팔 글인지」가
> 정해진 채로** 시작한다(`SKILL.md` 단계 1-0). 각도는 **A 제품형 / B 제작기형** 2계열.
> 🆕 **블로그 22 → 25편.** 크몽 상품 3개(진단 804972 · 제작 805177 · 브랜드커넥트 801624)가
> 전부 글을 갖게 됐고 서로 링크한다.
>
> ✅ **08-20 할 일은 전부 끝났다.** 색인 요청 7편 완료(사용자) · 짱샘의 책방 `detail` 최신화(`c583f67`).
> **지금 열려 있는 할 일은 없다.**
>
> 🆕 **앞으로 글을 쓰면 `/blog-post` 가 마지막에 「색인 요청할 URL」을 따로 뱉는다**(단계 9).
> 배포가 끝난 뒤에 구글 서치콘솔 `URL 검사 → 색인 생성 요청` / 네이버 `요청 → 웹 페이지 수집`에 넣으면 된다.
> 사이트맵은 이미 제출돼 있어 재제출 불필요.
>
> ❌ **하지 않기로 한 것**: 크몽 리뷰 확보 · 브랜드커넥트 `slug`/`detail` 추가.
> 이유는 아래 「2026-08-20」 끝의 표에 있다. **다시 제안하지 말 것.**
>
### 계속 유효한 규칙 (앞 회차에서 확정)

> - **이 레포의 스킬은 `/blog-post` 하나뿐이다**(08-19). **"2편"만 붙이면 2편**을 쓴다.
>   🔴 통합 이유가 「규칙이 두 벌이라 계속 어긋남」이니 **다시 쪼개지 말 것.**
> - 🔴 **네이버 블로그는 여기서 안 한다** — `jungaepro` 레포의 `/naver-blog` 가 한다(계정 `bsuha01`).
>   junominu 에 네이버용 스킬을 다시 만들지 말 것.
> - 🔴 **글을 쓰기 전에 「그 프로젝트 레포」를 훑는다**(`SKILL.md` 단계 1-A). 재료가 `detail` +
>   **레포** + 라이브 **세 곳**이고, 어긋나면 **지금 보이는 값은 라이브가, 제작 이야기는 레포가** 이긴다.
> - **이미 나간 글은 소급 수정하지 않는다.** 「낡은 값」은 앞으로 쓸 글에만 적용한다.
>
> 자세한 건 아래 「2026-08-19」 섹션.


> **"이제 뭐 해야 해?" → ① 아래 「답을 기다리는 3건」을 먼저 그대로 보여준다 ② 그다음 작업 순서를 안내한다.**
>
> ❌ **08-18 의 `/naver-post` 는 하루 만에 폐기됐다**(08-19). `jungaepro` 레포에 더 나은
> `/naver-blog` 가 이미 있었다. 아래 「2026-08-18」 섹션 전체가 무효이고 `keyword-map.md` 같은
> 파일도 **없다** — 네이버 블로그 관련 판단은 전부 `jungaepro/blog-content/context/` 가 단일 출처다.
>
> 🆕 **08-14 에 크게 움직인 것** — 블로그 14 → **18편**, 글 하단 문의 블록(AskBox) 신설,
> **구글·네이버 검색엔진 등록 완료**, RSS 피드 신설. 자세한 건 아래 「2026-08-14」 섹션.
> 사이트맵·RSS 제출까지 **양쪽 다 끝났다.** 남은 건 구글 URL 검사로 프리렌더 렌더 결과를 눈으로 보는 것 하나.
>
> 사이트 디자인은 전면 개편해서 **다 배포됐다**(팔로워 68, 조회 2만).
> 작업 순서: **모바일 실기기 확인 → 히어로 문구 감수 → ~~나머지 프로젝트 추가~~(✅ 08-05 완료, 17개) → 코칭 사례 시리즈 3편 → ~~블로그~~(✅ 08-14 착수, 스킬 2종 완비).**
>
> 🆕 **08-05 에 남긴 미결 2건** — 아래 「④ 게임 3종 배포 여부」·「⑤ 서이추 도우미 공개 노출」.

### 🙋 먼저 — 사용자 답을 기다리는 3건 (2026-08-03 세션 끝에 남긴 것)

> **"이제 뭐 해야 해?" 를 들으면 이 세 개를 먼저 그대로 보여줄 것.** (사용자 지시 2026-08-03)

**① 기억정원 사진 저장** — ✅ **확인 완료 · 조치 없음으로 결정**(2026-08-03)
코드 확인 결과 **사진은 클라우드로 간다.** 발행글(07-28)의 *"로컬에서만 남아 있도록"* 이 틀렸다.
- `lib/reminiscence/index.ts:107` → `supabase.storage.from('dp-reminiscence').upload(...)`
- 버킷은 **비공개**(`003_phase4_reminiscence.sql:90`, `public=false`) · 서명URL 1시간 · **인도 뭄바이**(국외 이전)
- 로컬 저장 경로는 **아예 없다**(FileReader·IndexedDB·localStorage 저장 0). 로그인 필수라 오프라인 모드도 없음.
- ✅ **"AI에 안 보낸다"는 맞다** — 힌트 텍스트만 보낸다(`lib/ai/claude.ts:158`).
- ✅ 개인정보처리방침·`projects.js` 는 **이미 정확하다.** 스레드 글만 틀렸다.
> **사용자 결정: 그냥 둔다**(정정 글 안 씀, 2026-08-03).
> 🔴 **다만 앞으로 기억정원 글에 "로컬에만 저장" 류를 다시 쓰지 말 것.** 쓸 수 있는 정확한 표현은
> **"사진은 AI에 보내지 않습니다"** 와 **"비공개로 보관되고 링크는 곧 만료됩니다"** 다.

**② 중개프로 링크** — ✅ **정리 완료**(2026-08-03). `https://www.jungaepro.com/` 하나로 통일.
서브도메인·apex 둘 다 쓰지 않는다. 테넌트 상단 메뉴 404 건은 `jungaepro` 레포 이슈로만 남긴다(퍼널 영향 없음).

**③ [연재 2화]** — 중개프로 정식 출시 후에나 쓸 수 있다. **지금은 대기**(시기 문제).

---

### 🔴 ① 모바일 실기기 확인 — 이번 개편에서 유일하게 눈으로 못 본 것
개편이 컸는데(히어로·섹션 카드·상단 바·움직이는 창) **PC에서만 확인했다.**
🚨 **Windows Chrome 은 창 폭 하한(약 500px)이 있어 `--window-size` 도 `resize_window` 도 모바일 확인에 못 쓴다**(이번에도 막혔다).
CDP `Emulation.setDeviceMetricsOverride` 를 쓰거나 **실제 폰으로** 볼 것. 특히:
- 상단 바 — 620px 이하에서 로고 위 / 내비 아래 가운데로 접힘
- 히어로 무대 창 — 터치 기기엔 마우스가 없어 기울기 없이 정적으로 보인다(의도됨)
- 섹션 카드 좌우 여백 · 대표작 1열 전환(620px)

### ② 히어로 문구 감수 — `ME.heroLines` 9개
움직이는 창에서 순환하는 **ask/reply 는 내가 프로젝트 설명만 보고 쓴 초안**이다.
실제로 AI에게 시켰던 말과 다르면 고쳐야 한다. `src/projects.js` 의 `heroLines` 에 다 모여 있다.
✅ 완성 줄은 손대지 말 것 — `slug` 로 `PROJECTS` 에서 이름·상태를 가져온다(`DONE_PHRASE`).

### ✅ ③ 나머지 프로젝트 추가 — **2026-08-05 완료 (9 → 17개)**
추가: 상세페이지 메이커 · 센스트랙 · 아이다이어리 · 브랜드커넥트 도우미 · 서이추 도우미 ·
무궁화 꽃이 피었습니다 · 슬라이딩 퍼즐 · 오델로. 카테고리가 5개 전부 채워졌다.
- ⚠️ 8개 모두 **`detail` 이 없다** → 카드만 나오고 `/p/<slug>` 상세는 없다. 프리렌더도 그대로 9개.
  메이킹 스토리를 쓸 때 `slug` + `detail` 을 같이 넣을 것(그 순간 `sitemap.xml` 도 갱신).
- ⚠️ **`heroLines` 는 자동 연동이 안 된다.** 히어로 창에 넣고 싶으면 따로 추가.
- `public/og.png` 를 **`17 PROJECTS`** 로 다시 떴다(카드 HTML은 스크래치패드, 커밋 안 함).

### 🙋 ④ 게임 3종 — **배포할까요? (답 대기)**
무궁화 · 슬라이딩 퍼즐 · 오델로는 완성됐는데 **공개 주소가 없다.** 지금은 `status: "live"` 인데
`url` 이 없어서 **카드를 눌러도 아무 일이 안 난다.** 셋 중 하나를 고를 것:
- ㉮ Vercel 에 올려 `url` 을 붙인다 ← 포트폴리오에선 이게 제일 낫다(눌러서 놀 수 있는 카드)
- ㉯ 링크 없이 그대로 둔다 (지금 상태)
- ㉰ 목록에서 뺀다
> ⚠️ 슬라이딩 퍼즐은 **Gemini 키가 번들에 인라인**된다(`Sliding-Puzzle/CLAUDE.md` 경고). 올리려면 키를 먼저 빼야 한다.

### 🙋 ⑤ 서이추 도우미 — **공개 포트폴리오에 그대로 둘까요? (답 대기)**
`Blog-mgr/tools/naver-blog-seoichoo/README.md`(2026-08-20 최상위에서 이동) 에 *"네이버는 자동화 도구로 로그인/이웃추가하는 행위를 약관으로 금지"* 라고
본인이 적어 두셨다. 개인용이라 판매·배포는 아니지만, **junominu.com 은 공개 포트폴리오**라 카드로 걸어 두면
그 도구를 홍보하는 모양이 된다. 그대로 둘지 / 문구를 순화할지 / 뺄지 판단이 필요하다.

### 🎨 2026-08-05 — 좌우 여백을 페이지 전체로 통일 + 프로젝트 8개 추가
**증상**: 스크롤하면 맨 위(상단 바·히어로)만 배경이 화면 끝까지 깔리고, 그 아래 섹션은 전부
좌우 여백이 있었다 → *"좌우측 끝은 여백으로 두기로 했는데 이 부분은 제대로 안되어 있네"*(사용자).
**실측**(1571px 뷰포트): `topbar`·`herowrap` = `0→1571`, 나머지(`sband`·`cta`) = `265→1305`.
**조치**: `.topbar` / `.herowrap` 을 `.sband` 와 같은 2단 구조로 바꿨다 —
바깥이 `max-width: var(--maxw)` + 좌우 패딩, 안쪽(`.topbar__inner` / `.herowrap > .page`)이 흰 카드
(테두리 · `--radius` · 점격자 · 파란 광). 점 격자 `::before` 도 안쪽으로 옮겼다(안 옮기면 여백까지 점이 번진다).
- 결과: **맨 위부터 풋터까지 전부 `265→1305`**, 가로 스크롤 없음(`scrollWidth == clientWidth`).
- `.page--top` 이 아래 여백을 갖고 `.hero { margin-bottom: 0 }` — 카드가 여백을 갖는 구조라 그렇다.
- ⚠️ CSS 주석을 고칠 때 **기존 블록의 `*/` 안쪽에 새 줄을 넣지 말 것.** 한 번 그래서 `.topbar` 규칙이
  통째로 무시됐다(상단 바만 그대로 풀블리드로 남아 있었다).

### 🎨 이전 세션 — 디자인 전면 개편 (2026-08-03)
`DESIGN.md` 를 **오늘의집 → 토스**로 교체하고 구조까지 갈아엎었다. **상세는 `CLAUDE.md` §디자인.**
핵심만: 섹션 10→6 · 대표작 2열 + 카테고리 컴팩트 · 내비를 상단 바로 · 그림자 전부 제거 ·
**옅은 캔버스 위 흰 테두리 카드**(네이버 홈 방식) · 색은 프롬프트 밴드(옅은 파랑)와 CTA(먹색) 두 곳만.
- 🔴 **히어로는 여섯 번 갈아엎었다.** 되돌리기 전에 `CLAUDE.md` 의 시도 이력 표를 읽을 것.
  최종: 배지 → 문장 헤드라인(이름 아님) → 한 줄 → CTA 하나 → **마우스 따라 기울어지는 창**(한국어 지시문 타이핑 → AI 답변 → 만드는 과정 체크 → 완성).
- 🔴 **개발자 용어를 세 번에 걸쳐 걷어냈다** — 커밋/commits/React·Next.js 태그/System Architect/SaaS/배포.
  **새 문구를 넣기 전에 물을 것: "코딩 한 번도 안 해본 사람이 이 단어를 알까?"**

### 🔗 중개프로 링크는 **`https://www.jungaepro.com/`** 하나뿐이다
🔴 **서브도메인(`jungae.jungaepro.com` 등)을 쓰지 말 것.** 그건 입점 사무소의 손님 페이지다.
🔴 **apex(`jungaepro.com`)도 쓰지 말 것** — www 로 리다이렉트되므로 처음부터 `www` 를 붙인다.
스레드 CTA·`projects.js`·앞으로 쓸 글 전부 이 주소로 통일(사용자 지시 2026-08-02, www 확정 2026-08-03).
옛 주소 `smart-sjhome.vercel.app` 은 폐기 — `drafts/`·`threads-log.md` 의 옛 UTM 링크는 과거 기록이라 그대로 둔다(새 글에서 복사 금지).

### 🚨 아직 확인 안 된 것 — 기억정원 사진 저장 방식이 문서와 다르다
- 07-28 발행글에서 **"사진은 클라우드로 안 보냅니다 … 로컬에서만 남아 있도록 한거에요"** 라고 공개적으로 말했다.
- 그런데 `src/projects.js` 의 features·challenges 는 **`사진은 AI 미전송·비공개 버킷+서명URL`**(= 클라우드 저장)이다.
- **둘 중 하나가 낡았다.** 공개 글에서 개인정보 처리를 언급한 것이라 **실제 구현 → 약관/개인정보처리방침 → `projects.js`** 순으로 맞출 것.

### 🏆 그 다음 — 터진 글 재활용 (8월 중순)
- **아이복지모아 「치료실 2,270곳 지도」**(좋아요 149·리포스트 17)가 유일한 아웃라이어다. `threads-log.md` 하단 표에 재활용 후보 3개를 적어뒀다: ① 지도에서 실제로 찾는 장면(부모 1인칭) ② 대기 걸렸을 때 대안 찾기 ③ 2,270곳을 어떻게 모았나(계정 소개 맥락).
- **리포스트가 17이라는 게 핵심 힌트** — 남에게 알려주고 싶은 정보였다. 다음 글도 "퍼가고 싶은 정보" 성격으로 설계할 것.

1순위(라우트별 메타·OG 프리렌더)·2순위(홈 첫인상)·3순위(번들 다이어트) 모두 **완료**. 사이트 코드 쪽 대기열은 비었고, 남은 건 **콘텐츠 + 채널 운영**이다.

### 🧵 스레드 글 — **대기열 비었음. 다음은 코칭 사례 시리즈 3편**
- 07-27~08-02 에 **12편** 발행. `drafts/` 에 미발행 초안 **없음**.
- 🔴 **판매 글은 이 계정에서 안 나간다** — 08-01 래피드 코칭 판매 글이 **조회 25**로 죽었다(같은 계정이 일주일 전 좋아요 161). 가격·환불·외부 결제 링크가 한 화면에 있으면 사람도 알고리즘도 광고로 읽는다.
  → **판매는 프로필과 답글에서.** 글은 "안 파는 사례 글"로 간다(사용자와 합의).
- ✅ **코칭 사례 시리즈 시작** — 필라테스(좋아요 2·답글 5) · 엔터 대표(전면 개작) 발행. 남은 후보:
  ① **크몽에서 실제로 파는 분** ← 가장 셈. 캡처·동의 필요
  ② **"웹 서비스가 뭔지 몰라서" 막히는 이야기** ← 동의 불필요, 바로 쓸 수 있음
  ③ **0호 후기** ← 나오면 이 시리즈의 결승점
- 📌 **0호 3명은 스레드보다 전자책 구매자가 빠르다** — `lecture/latpeed/README.md` §9 에 직접 써두심(*"일주일이면 채울 수 있는 숫자"*).
- ⛔ 마감·알림톡 각도는 아직 금지 — 카카오 템플릿 '광고성' 반려로 심사 중.
- ⚠️ **[연재 2화] 미이행** — 07-23 에 *"출시하면 첫 결제 결과를 올리겠다"* 고 약속. 중개프로 정식 출시 후 반드시.
- 📌 **발행 리듬**: *"묻히더라도 주기적으로 계속 올린다"*(사용자 방침). **발행을 미루라고 조언하지 않는다.**

### 🗣️ 팔로워 레버 — 스레드 답글 엔진 (**12회차까지 완료 · 누적 70계정**)
- **`/threads-reply` 스킬**로 "댓글 써줘" 한마디면 타깃 검색 → 자가검증 답글 → 승인 카드 → 발행이 돈다. 캡처를 주면 **대댓글 추천안 3개**(모드 B).
- **누적 70개 계정**(`reply-log.md`) — 스캔 때 자동 제외되므로 재답글 걱정 없음. **리듬 유지가 전부다.**
- 🗑️ **@nangman_muk 폐기 완료**(2026-08-02) — 승인본이 사실과 달라 보류했던 건. **미결 없음.**
  → **규칙 승격: 사실이 틀린 답글은 승인 토글이 켜져 있어도 발행하지 않는다.** 임의 수정도 안 하고, 선택지를 드리고 기다린다.
- 🎯 **코칭 시리즈 기간엔 초보 계정에 답글 집중** — `build_alfie`·`half.nomad`·`helen.after50` 결.
  **25뷰를 만든 건 글이 아니라 씨앗 청중이었고, 그건 답글로만 바뀐다.**
- ✅ **발행 절차 확정**(`card-ui.md`): 전송 버튼 `svg[aria-label="답글"]` · `fill`+`send` 한 호출(`window.go`) · 🚨 **한글 이스케이프 금지**(7·8회차 오타 사고 → 이후 0건) · 발행 후 승인본 대조 · 수정 모달은 `[role="dialog"]` 안에서 선택.
- 🚫 **대댓글 체인 금지**(사용자 지시 07-28) — 답글은 **1왕복에서 끝낸다.** 상대가 답해도 다시 달지 않는다. 모드 B는 명시 요청 시에만.
- 🔎 **검색어 교훈**: `공인중개사`는 매물 광고가 대부분 → **`중개사무소`로 바꾸니 현업 중개사의 진짜 에피소드가 잡혔다**(11회차). 중개사 니치 기본 검색어를 `중개사무소`로.

### 🟢 4순위 — 블로그 콘텐츠 ← **여기부터**
- 글 **3개, 마지막이 2026-06-04**. 스레드의 🔧빌드로그·💡인사이트를 **받아주는 심화 글 경로가 비어 있다**. 스레드(짧게) → 블로그(깊게) → `/contact` 흐름을 만들어야 한다.
- ✅ **글 쓰는 도구는 마련됨**(2026-07-23) — **`/blog-post` 스킬**(`.claude/skills/blog-post/`). 프로젝트 slug 하나 주면 끝. 2,000~3,000자 · 앱 캡처 3~5장 · FAQ 3문항 · 표 1개 · 사이트맵·빌드 검증까지.
- **남은 건 실행뿐.** 프로젝트 9개 = 글 9편 로드맵이 스킬 §6에 표로 있다. 유력 순서: `petphoto`(실결제 E2E) → `bokjimoa`(공공데이터) → `transcripto`(데스크톱 배포).

### ✅ 모바일 가로 넘침 — **실재하지 않음** (2026-07-22 확인, 수정 불필요)
- CDP `Emulation.setDeviceMetricsOverride`로 뷰포트를 강제해 실측: **320·360·390·430px 전부 `scrollWidth == viewport`, 넘치는 요소 0개.**
- 🚨 **재발 방지 — Windows Chrome은 `--window-size` 폭에 하한(약 500px)이 있다.** `--window-size=430`을 줘도 **512px 뷰포트로 렌더한 뒤 PNG만 430으로 크롭**한다 → 오른쪽이 잘린 것처럼 보여 "넘침"으로 오진하기 쉽다.
  → **모바일 확인은 `--window-size`로 하지 말 것.** CDP Emulation을 쓰거나(스크립트 예: 이 세션의 `overflow-probe.mjs`), 최소 500px 이상 폭에서만 판단할 것.

### ✅ Tally 숨김필드 — **생성 완료** (2026-07-23, 폼 정의에서 실측 확인)
- 폼 `b5EKOL` 정의에 `hiddenFields: [{name:"type"}]` + `[{name:"src"}]` **둘 다 존재**. `Contact.jsx`가 보내는 키와 이름 일치 → 이제 문의 응답에 **문의유형 + 유입경로**가 함께 기록된다.
- `src` 값: `threads/<캠페인>`(UTM) → 없으면 `l.threads.com`(referrer) → 없으면 `direct`. **first-touch**라 사이트 안을 돌아다녀도 최초 유입이 유지된다.
- ⚠️ 다만 **UTM을 붙여 올린 글이 있어야** `threads/<캠페인>` 단위 귀속이 생긴다(아래 참조).

### ⏸️ 사이트 밖(사용자 직접 해야 하는 일)
- **동료 링 20~30계정 리스트업** — 스레드 전략 §5, 1~2주 과제 중 유일하게 남음.
- 스레드 글 링크에 **UTM** 붙이기: `?utm_source=threads&utm_campaign=<글슬러그>`
  - 예: `https://www.junominu.com/p/petphoto?utm_source=threads&utm_campaign=petphoto-anchor`
  - 이걸 해야 Tally `src`와 Vercel Analytics **UTM Parameters** 탭이 글 단위로 쪼개진다. 안 붙이면 전부 `l.threads.com` 한 덩어리.

---

## 2026-08-20 — 🔴 블로그의 목적을 바로잡음 + 크몽 홍보 글 3편 (블로그 22 → 25편)

> **이 세션에서 제일 중요한 건 글 3편이 아니라 전제가 바뀐 것이다.**

### ① 🔴 「블로그의 목적은 제품 홍보다」 — 스킬 전제 수정

`/blog-post` 가 *"홍보 톤 금지. 제품 자랑이 아니라 **만든 과정의 기록**이다"* 라고 못박고 있었다.
사용자 지적으로 **틀린 전제**임이 드러났다 — junominu.com 자체가 나와 내 제품을 알리려는
프로젝트고, 블로그도 그 목적에 복무한다. **제작기는 목적이 아니라 수단 중 하나다.**

| | 전 | 후 |
|---|---|---|
| 목적 | 만든 과정의 기록 | **읽은 사람이 판매처로 넘어가게** |
| 금지 | 홍보 톤 | **과장·거짓**(없는 기능·부풀린 숫자) |
| 각도 | 삽질·방법·결정 3종 | **A 제품형** / **B 제작기형**(3종은 B 아래로) |
| 외부 링크 | 공식 문서만 | **내 제품 판매처는 넣는다** |

- **단계 1-0 신설** — 「무엇을 팔 글인지」(파는 것·착지점·지금 살 수 있나·독자·**들어오는 문**)를
  먼저 정한다. **안 정해지면 쓰지 않는다.**
- 🔴 **제품명으로는 아무도 검색하지 않는다**는 제약은 유지했다. 제품형이어도
  **문 앞에는 독자의 문제를 놓고 안을 제품 이야기로 채운다.** 순수 광고 글은 아무도 못 찾는다.
- **「이런 분께는 맞지 않습니다」를 제품형 필수 항목**으로 박았다. 광고 글에서 신뢰가 붙는
  거의 유일한 자리다(짱샘의 책방 상세페이지가 쓰는 장치).
- 이미 나간 22편은 소급 수정하지 않는다.

### ② 크몽 상품 3개 = 글 3편 (전부 A 제품형)

**크몽 상품에는 `/p/<slug>` 상세가 없어도 된다** — CTA를 판매처로 직결한다.

| gig | 상품 | 글 | 가격 |
|---|---|---|---|
| **804972** | (AX 현장 진단) 전국 출장 업무 자동화 로드맵 | `ax-onsite-diagnosis` | 39/89/159만원 |
| **805177** | 매일 알아서 도는 업무자동화 프로그램 제작 | `work-automation-build` | 5/19/39만원 |
| **801624** | AI 브랜드커넥트 쇼핑커넥트 블로그 초안 프로그램 | `brandconnect-helper` | **59,000원 단일·영구** |

**세 글이 서로 링크한다**: 진단 ⇄ 제작 → 브랜드커넥트.
진단 글의 *"개발은 그다음입니다"* 가 제작 글로 착지하고, 제작 글의 *"무엇을 자동화할지
못 정하셨다면"* 이 진단 글로 되돌아간다.

각 글이 쓴 **들어오는 문**(제품명이 아니라 문제):

- 진단 — *"견적서는 받았는데 무엇을 만들지는 우리도 모른다"*
- 제작 — *"매일 아침 같은 파일을 같은 순서로 정리한다"* (+ 제목에 **5만원부터** 노출)
- 브랜드커넥트 — *"원고는 밀리는데 자동 발행은 계정이 걱정된다"*

🔴 **브랜드커넥트 글의 축은 「없는 기능」이다.** H2 하나를 통째로 *"기능은 다른 프로그램이
더 많습니다"* 에 쓰고, 자동 발행·예약 발행·다계정·대량 발행이 없는 이유가 **가격이 아니라
계정 안전**임을 밝힌다. 크몽 상세의 *"위 네 줄이 필요하시면 다른 상품을 사십시오"* 를 그대로 옮겼다.

### ③ 이미지 — 두 가지 방식이 생겼다

| 방식 | 언제 | 이번 사례 |
|---|---|---|
| **직접 제작** (HTML → Chrome 헤드리스) | 서비스라 화면이 없을 때 | 진단·제작 글 각 3장 |
| **크몽 상세 이미지 재사용** | 이미 만들어 둔 게 있을 때 | 브랜드커넥트 3장 |

- 직접 제작은 **사이트 디자인 토큰**(Pretendard·토스 팔레트·그림자 없음·1px 테두리·라운드 18px)에
  맞춰 그린다. 첫 장은 **1200×630**(og:image).
  → HTML 원본은 스크래치패드에 있고 커밋하지 않는다. 고치려면 다시 만든다.
- 🔴 **재사용이 더 낫다.** 브랜드커넥트는 `BrandConnect/dist/kmong/` 에 **실제 앱 화면**
  (`demo-frame-*`)과 비교표(`detail-03-비교`)가 이미 있었다. 새로 그리는 것보다 세다.
  **그 레포는 복사만 하고 건드리지 않았다**(읽기 전용 규칙).

### 커밋 (전부 push 완료)

| 커밋 | 내용 |
|---|---|
| `b6ee1ec` | 스킬 전제 수정 — 블로그의 목적은 제품 홍보 |
| `c97b959` | AX 현장 진단 글 + 자체 제작 이미지 3장 |
| `7d1304c` | 업무자동화 제작 글 + 자체 제작 이미지 3장 |
| `1a6c9a0` | 브랜드커넥트 글 + 크몽 이미지 3장 재사용 |

### 🔴 다음 세션이 이어서 할 것

1. **색인 요청 7편이 밀려 있다.** 8/14에 4편 넣은 뒤로 안 했다. 구글 서치콘솔 `URL 검사 →
   색인 생성 요청` / 네이버 서치어드바이저 `웹 페이지 수집`:
   ```
   bokjimoa-verdict-limits · jungaepro-ai-quota · petphoto-toss-key-mixup
   jjangsaem-53-to-6-books · ax-onsite-diagnosis · work-automation-build · brandconnect-helper
   ```
   사이트맵은 8/14에 제출해 뒀고 25편 전부 등재돼 있으니 **재제출은 불필요**하다.
   ⚠️ **앞으로는 글 쓸 때마다 바로 넣을 것.** 밀리면 "어디까지 했더라"를 매번 다시 계산하게 된다.
2. ✅ **짱샘의 책방 `detail` 최신화 완료** (`c583f67`) — 커밋 616→**840**, cover 를 44권 시절 화면에서
   지금 카탈로그로 교체, timeline 9줄(7/30 신간 ~ 8/19) 추가, summary·features·retro 에 6권 전환 반영.
   🔴 **옛 `jjangsaem-cover.png` 는 지우지 말 것** — 블로그 글의 before 이미지로 쓰이고 있다(주석으로도 박아 뒀다).
   안 쓴 `jjangsaem-hero-6books.png` 는 삭제했다(사은품 팝업이 화면을 가려 못 씀).

### ✅ 이 회차에 끝난 것

- **색인 요청 7편** — 사용자가 완료.
  🆕 앞으로는 `/blog-post` 가 **단계 9에서 「색인 요청할 URL」을 자동으로 뱉는다.** 밀릴 일이 없다.
- **짱샘의 책방 `detail` 최신화** — 위 2번.

### ❌ 하지 않기로 한 것 (2026-08-20 사용자 결정)

| 안 함 | 왜 |
|---|---|
| **크몽 리뷰 확보** | 리뷰 0건은 그대로 둔다. 후기를 만들려고 실제 돈을 쓸 여유가 없다. (내가 제안한 건 기존 구매자 4명에게 부탁하는 쪽이었지만, 부담스러우면 안 한다) |
| **브랜드커넥트에 `slug`·`detail` 추가** | 🔴 **내 제안이 틀렸다.** `detail` 을 넣으면 카드 클릭이 **크몽이 아니라 `/p/` 내부 페이지로 바뀐다** — 크몽으로 바로 보내려는 의도와 정반대다. 크몽에서 파는 서비스는 카드가 곧장 판매처로 가는 게 맞다. **다시 제안하지 말 것.** |

---

## 2026-08-19 — 스킬 정리: 5개 → 2개 (스레드 2종 삭제 + 블로그 2종 통합)

> 요청 두 건: *"스레드용 스킬은 삭제해도 좋아"* / *"`/blog-post`와 `/add-blogs-to-project`는 편수만 다르고 완전히 같은 스킬인가?"*

### ① 스레드 스킬 2종 삭제

`threads-post` · `threads-reply` 폴더 통째로 제거(45개 파일). **커밋 안 된 변경 0건**이었으니 전부 git에 남아 있다 —
되살리려면 `git restore .claude/skills/threads-post .claude/skills/threads-reply`, 커밋 후엔 **`de0a05e`** 에서 꺼낸다.

같이 사라진 **기록물**(스킬 지침이 아니라 자산이라 따로 적어 둔다):
`threads-log.md`(862줄 발행 이력·KPI) · `voice-profile.md`(675줄 **문체 학습 누적**) · `queue/PUBLISHED.md` · `drafts/` 22개 · `reply-log.md`(199줄).
> 계정(@solopreneur.jm, 팔로워 68)은 그대로 살아 있다. 스킬만 없앤 것이다.

끊어진 참조 정리: `CLAUDE.md` 2줄 삭제 · `blog-post`/`add-blogs` 참고표 행 교체 · **`WORKLOG.md` 맨 위에 경고 추가**
(아래 07-22~08-05 기록에 나오는 그 파일들을 **찾으러 가지 말라**는 표시. 기록 자체는 이력이라 그대로 둔다).
⚠️ 아직 남은 것: 스레드 관련 **미완료 체크박스 2건**(앵커 반응 수치 기록 · 다른 프로젝트 앵커 스레드) — 실행 수단이 없어졌다.

### ② 블로그 스킬 2종 → 1종 통합

**질문에 답하려고 실제로 측정했다.** 처음엔 "~85% 복제"라고 어림했는데 **틀렸다.**

| | 값 |
|---|---|
| `blog-post` / `add-blogs-to-project` | 고유 252줄 / 267줄 |
| **줄 단위 완전일치** | **76줄 = 약 30%** |

그런데 **나머지 70%의 대부분이 "다른 규칙"이 아니라 "같은 규칙을 줄여 쓴 것"** 이었다.
실제로 내용이 다른 건 **단계 2(재료 2등분)·단계 3(각도 쌍)·상호 링크** 셋뿐.

🔴 **그리고 이미 어긋나 있었다** — `AskBox` 가 `add-blogs` 에 6번, `blog-post` 에 **0번**.
`/blog-post` 로 쓴 글은 본문에 문의 문구를 넣어 하단 블록과 **두 번 말할 수 있었다.**

원인은 **출생 시점 차이**였다(git으로 확인):

```
07-23  5960343  blog-post 신설
08-14  099a792  AskBox + add-blogs 신설      ← 둘이 같은 커밋에서 태어남
08-15  6a3aeee  add-blogs 에 "라이브 우선"
08-15  83bac6b  blog-post 에도 "라이브 우선"  ← 하루 뒤 따라잡기
   ?           blog-post 에 AskBox → 못 따라잡음
```

**새 규칙은 늘 `add-blogs` 에 먼저 들어가고 `blog-post` 가 뒤늦게 따라오는 구조.**
게다가 문장이 미묘하게 달라 **grep으로 "이 규칙이 두 곳에 있나"를 찾을 수 없다** — 단순 복붙보다 나쁘다.

**조치**: 내용은 **`add-blogs-to-project` 기준**(더 새롭고 규칙이 갖춰짐), 이름은 **`/blog-post`**(자연스럽고 `naver-post`·`CLAUDE.md` 가 이미 참조).
편수를 인자로 흡수 — 그냥 부르면 1편, "2편"이면 단계 2·3이 추가로 돈다(**🔵 [2편]** 표시로 구분).
1편 전용이던 **각도 3종 표(삽질형/방법형/결정형)** 도 살려서 합쳤고, 빠졌던 **AskBox 규칙**을 채웠다.
`add-blogs-to-project` 폴더는 삭제.

- §8 글감 로드맵은 **정적 표를 버리고 명령으로 바꿨다**(`ls src/posts/` + `grep ^title:`) — 표는 금방 낡는다.
- 🔴 `SKILL.md` 머리와 `CLAUDE.md` 에 **"다시 두 파일로 쪼개지 말 것"** 을 박아 뒀다.

### 남은 스킬 (5 → 2)

| 스킬 | 무엇 |
|---|---|
| `/blog-post` | junominu.com `/blog` — 메이커 독자·구글. 1편 또는 2편 |

### ③ `/naver-post` 도 삭제 — 이미 있는 걸 중복 구현한 것이었다

중개프로 프로젝트를 훑다가 **`jungaepro` 레포에 네이버 블로그 시스템이 이미 있는 걸 발견했다.**

```
jungaepro/.claude/skills/naver-blog/SKILL.md     280줄
jungaepro/blog-content/context/                  팩트시트·키워드·어근대장·페르소나·보이스
jungaepro/naver-blog/*.txt                       발행 완료 2편(08-08 홈페이지 · 08-09 매물관리)
```

**08-18에 내가 "최우선 후보"라고 제안한 두 키워드가 이미 써서 발행까지 끝난 상태였다.**
그리고 기존 것이 전 항목에서 낫다 — `product-facts.md`(🔴 **"없는 기능" 금지 목록** 포함) ·
**어근 90일 쿨다운 대장** · **계정 분리**(`bsuha01` 중개 / `bsuha` 육아) · **검색+홈피드 이원 전략** ·
마크다운 0건 순수 txt · Gemini 실장면 사진 · refresh/feedback 모드.
내 `naver-seo.md` 는 '저품질'을 실재하는 판정처럼 썼는데, 저쪽은 *"네이버 공식 개념이 아니다"* 라고 정확히 적어 뒀다.

**조치**: 이식할 것 3개만 옮기고 `/naver-post` 삭제(사용자 지시). **발행은 `jungaepro` 에서 한다.**

| 이식한 것 | 왜 |
|---|---|
| **§junominu 대조** (SKILL.md) | 저쪽 스킬이 junominu.com 에 중개프로 글 3편이 나가는 걸 **몰랐다.** 실제로 한 번 겹쳤다(네이버 08-08 홈페이지 ↔ junominu 08-14 `homepage-included`) |
| **`기준연도`·`노출확인` 열** (keyword-ledger) | `refresh`·`feedback` 모드가 판단할 **누적 데이터가 없었다** |
| **자동 발행 불가 근거** (SKILL.md) | 왜 손으로 붙여넣는지 기록이 없어 나중에 "자동화하자"가 나올 수 있었다 |

> 🔴 **junominu 에 네이버용 스킬을 다시 만들지 말 것.** 재료가 `jungaepro` 에 있다.

### ④ 채널 분담 확정 + `/blog-post` 에 「레포 훑기」 추가 (사용자 지시)

**정책**: 🔴 **네이버 블로그는 junominu 에서 만들지 않는다. 각 프로젝트 레포에서 한다.**
재료(팩트시트·키워드·페르소나)가 그 레포에 있고, 계정도 제품별로 갈리기 때문이다.
junominu 는 **자기 블로그(`/blog`)만** 쓰고, 그건 `/blog-post` 가 한다.

**그런데 junominu 블로그는 「남의 프로젝트 이야기」를 쓴다.** 그래서 `/blog-post` 단계 1에
**단계 1-A 프로젝트 레포 훑기**를 넣었다 — 재료가 **두 곳 → 세 곳**이 됐다.

| 재료 | 무엇 | 언제 이기나 |
|---|---|---|
| `projects.js` `detail` | 손으로 적은 요약 | **목차로만.** 나머지 둘과 어긋나면 진다 |
| 🆕 **프로젝트 레포** | `CLAUDE.md`·git log·`docs/`·`package.json` | **제작 이야기** — 막힌 점·결정·커밋 수·날짜 |
| 라이브 화면 | 지금 화면 | **지금 보이는 값** — 요금·개수·문구 |

**근거는 오늘 중개프로 실측이다:**

| | `detail` | 레포 |
|---|---|---|
| 커밋 | 200 | **384** |
| 매물 종목 | 9개 | **10개** |
| AI 회수·매물당 사진·운영사 | **없음** | Free 10/Basic 150/Pro 500 · 3/10/30장 · 짱샘에듀 |
| 입지분석·시세·공동중개 | **실기능처럼 기재** | 🔴 **일부 목업** |

반대로 레포엔 특약 104건, 무효 특약 10패턴(강행규정 위반), 토지거래허가구역(서울 25개 자치구),
확인설명서 3종 자동선택 함정 같은 **글감이 통째로** 있었다. **`detail` 만 봤으면 하나도 못 썼다.**

- 🔴 **레포 `CLAUDE.md` 가 최고의 글감** — *"코드를 읽어도 알 수 없는 것(함정·확정 결정)"* 만 모은 파일이라
  **한 줄이 곧 글 한 편의 씨앗**이다. 11개 프로젝트에 있다(`i-talk`·`LLM-Service-Transcripto` 는 없음 → git log·docs 로).
- **폴더 매핑표**(slug → `D:\Claude-prj\<폴더>`) 를 `SKILL.md` 단계 1-A 에 넣었다.
- `blog-content/context/product-facts.md` 가 있으면 **「없는 기능」 목록을 반드시 읽는다**(목업 방지).
- `naver-blog/*.txt` 가 있으면 제목을 훑어 **소재 겹침**을 피한다.
- 🔴 **다른 레포는 읽기 전용.** 수정·커밋 금지, 산출물은 junominu 안에만.
- 「낡은 값」 보고표에 **`레포` 열**을 추가했고, **목업을 실기능처럼 적어 둔 경우**를 필수 보고 항목으로 넣었다.

### 남은 스킬 (5 → 1)

| 스킬 | 무엇 |
|---|---|
| `/blog-post` | **이 레포의 유일한 스킬.** junominu.com `/blog` — 메이커 독자·구글 |
| ~~`/naver-post`~~ | → `jungaepro` 의 `/naver-blog` 로 일원화 |

### ⑤ 새 스킬로 글 1편 — 짱샘의 책방 「53권 → 6권」 (블로그 21 → **22편**)

**개편한 `/blog-post` 의 첫 실사용이고, 단계 1-A(레포 훑기)가 곧바로 값을 했다.**

- 파일 `src/posts/2026-08-19-jjangsaem-53-to-6-books.md` · 2,239자 · 각도 **결정형**
  (기존 `jjangsaem-refund-flow` 가 삽질형이라 갈랐다) · FAQ 3 · 표 1 · JSON-LD 2개
- 사용자 지시로 **기술 얘기를 전부 뺐다** — 초안은 크론·hidden 플래그 쪽으로 가고 있었는데
  *"사상·철학, 아이 몸 읽기라는 큰 그림으로 전환"* 으로 다시 잡았다. 🔴 **이 프로젝트 글은 그 축이 맞다.**
- 글의 축: 목록이 늘수록 *"이것저것 다 냈다"* 로 읽힘 → **고르기 도우미 3종을 붙여야 했던 것 자체가
  기능 부족이 아니라 목록 구조가 틀렸다는 신호** → 첫 문장의 동사 `풀어드립니다 → 읽습니다` →
  카테고리(무엇이 있나) → **STEP 1 몸읽기 / STEP 2 회복 루틴**(무엇부터 하나) → 대가는 매출 62.8%

**재료가 어디서 왔나** (단계 1-A 검증):

| 출처 | 무엇을 줬나 |
|---|---|
| `projects.js` `detail` | 목차뿐. **타임라인이 4/16에서 끊겨 6권 전환이 통째로 없다** |
| 🔴 레포 `ebook/jjangsaem-bookshop` | **글의 전부.** `docs/catalog-focus-6books-2026-07-31.md`(371줄)에 왜·무엇을·대가가 다 있었다 — 매출 62.8%, 구매자 439명 중 326명, 확정 정책 |
| 라이브 `jjangsaem.com` | 지금 6권·STEP 구조·히어로 카피·「책방 목록 외」 안내 |

> **`detail` 만 봤으면 이 글은 존재할 수 없었다.** 레포 훑기를 넣은 판단이 첫 판에 증명됐다.

**이미지 3장** — 기존 `jjangsaem-cover.png`(2026-06-04 캡처)에 **44권 배지·옛 부제·고르기 도우미 3종**이
그대로 찍혀 있어 **before 로 그냥 쓸 수 있었다.** after 2장은 새로 캡처.
⚠️ `jjangsaem-hero-6books.png` 는 사은품 팝업이 가운데를 가려 **안 썼고 커밋에서도 뺐다**(untracked로 남아 있음 — 지워도 무방).

### 🚨 Chrome 헤드리스 캡처 함정 2개 (이번에 둘 다 밟았다)

`SKILL.md` 단계 5에도 넣었다. 다음 세션이 또 밟지 않게.

1. **`--screenshot` 에 상대경로를 주면 `exit 0` 인데 파일이 안 생긴다.** 조용히 실패한다.
   → **Windows 절대경로**로 준다: `--screenshot="D:\\Claude-prj\\junominu\\public\\shots\\x.png"`
2. **`--virtual-time-budget` 없이 돌리면 2분 타임아웃에 걸린다**(SPA가 로드 완료 신호를 안 준다).
   → `--headless=new --virtual-time-budget=10000` 을 같이 준다.

### 커밋 (둘 다 push 완료)

| 커밋 | 내용 |
|---|---|
| `ff7ccd9` | 스킬 5 → 1 정리 + 단계 1-A 신설 (48 files, -5577줄) |
| `bf24a0b` | 짱샘의 책방 글 1편 + 이미지 2장 + sitemap |

`jungaepro` 쪽 이식분 3건은 **다른 세션이 `a306403` 에 섞어 이미 push** 했다(로그인 버그 수정과 한 커밋).
내용은 정상 반영 확인.

### 🔴 다음 세션이 이어서 할 것

1. **`projects.js` 의 짱샘의 책방 `detail` 갱신** — 이번 글에서 드러난 낡은 값:

   | 항목 | 지금 `detail` | 실제 |
   |---|---|---|
   | `commits` | 616 | **840** |
   | `timeline` | **2026-04-16에서 끝남** | 8/19까지 활동 — **6권 전환(7/31)·묶음 이용권·월 구독이 통째로 누락** |
   | `features` | 53권 시절 구성 | **6권 + STEP 1·2 체제** |
   | `summary` | 기능 나열 | 지금 정체성(**몸 읽기 포지셔닝**)이 빠짐 |

   → 고치면 `/p/jjangsaem` 상세 페이지도 같이 살아난다.
2. **`jjangsaem-hero-6books.png` 처리** — 안 쓴 캡처. 지울지 둘지 미정.
3. (선택) 배포본에서 **공유 카드·리치 결과** 확인 — og:image 가 6권 STEP 화면인지, FAQ 3문항이 잡히는지.

---

## 2026-08-18 — 네이버 블로그 채널 신설 (`naver-post` 스킬) — ❌ **폐기됨**

> 🔴 **이 섹션 전체는 하루 만에 무효가 됐다**(08-19). `jungaepro` 레포에 **더 나은 시스템이 이미 있었다** —
> 조사 전에 다른 레포를 확인하지 않아서 중복 구현했다. `/naver-post` 는 삭제됐고 아래 파일들은 **없다**
> (`naver-post/SKILL.md`·`naver-seo.md`·`keyword-map.md`·`queue/`). 찾으러 가지 말 것.
> **네이버 블로그는 `jungaepro` 의 `/naver-blog` 가 한다.** 자세한 건 위 「2026-08-19 ③」.
> 아래는 그때 조사한 내용이라 기록으로만 남긴다 — 다만 **키워드 후보·주축 판단은 `jungaepro/blog-content/context/` 가 단일 출처**다.

> 요청: *"네이버 블로그도 무시할 수 없는데 매일 수동은 부담스럽다. 최대한 자동화하고 싶다."*
> **결론: 발행 자동화는 포기하고, 제작을 100% 자동화했다.** 실제로 손 쓰는 시간은 **주 30분**.

### 왜 자동 발행을 안 하나 (조사 결과)

| 사실 | 출처 |
|---|---|
| **네이버 글쓰기 API는 2020-05-06 종료.** 사유가 *"광고성 글 대량 제작·게재 차단"* | [뉴시스](https://www.newsis.com/view/NISX20200413_0000992012) |
| 티스토리 Open API도 종료 → 우회로 없음 | [티스토리](https://tistory.github.io/document-tistory-apis/) |
| 유사문서·상업성 짙은 글·부자연스러운 활동 = **저품질** 대표 원인 | [TILNOTE](https://tilnote.io/en/pages/69ce9379a5dad016ee58c5b9) |

즉 **자동화의 목적(네이버 검색 유입)을 자동화의 수단(자동 발행·복붙)이 죽인다.** 아낀 시간을 노출 0으로 돌려받는다.
→ **제작은 Claude 100% / 발행은 사람 + 네이버 공식 예약 발행.** 주 1회 앉아 3편 예약하면 그 주는 끝.

### 🔴 주축 = 중개프로 하나 (사용자 지시)

C-Rank는 **블로그 단위** 신뢰도라 *한 주제에 누적*이 있어야 오른다([로카포스팅](https://locaposting.com/blog/naver-crank-dia-algorithm)).
프로젝트 17개를 골고루 쓰면 **51편을 써도 "주제별 3편짜리 잡블로그"** 다.
→ **중개프로 전용 블로그**로 확정. 다른 프로젝트는 섞지 않는다(필요해지면 블로그를 따로 판다).

🔴 **다만 주축은 "중개프로"가 아니라 「공인중개사 실무」다.** 이걸 놓치면 블로그가 죽는다:

| | ✕ | ○ |
|---|---|---|
| 주제 | 중개프로라는 **제품** | 공인중개사가 겪는 **업무** |
| 20편 나오나 | 5편이면 할 말 끝 | **100편도 나온다** |
| 네이버가 보기에 | 광고 블로그 → 저품질 | 업계 정보 → C-Rank 상승 |
| 검색어 | "중개프로"(아무도 안 침) | **"중개사무소 개설등록 서류"** |

**제품은 결론이지 주제가 아니다.** 업무를 20편 풀어준 뒤 마지막에 한 줄이면 된다.

- **독자가 전문가**라 발달장애 부모 같은 "정보 갈증" 독자와 다르다 → 아는 걸 설명하면 즉시 신뢰를 잃는다.
  공인중개사는 **본인들이 네이버 블로그를 마케팅 도구로 쓰는 집단**이라 경쟁도 세다 → **긴 실무 키워드만** 노린다.
- **출시 전이라 오히려 지금이 맞다** — 중개프로는 `building`(베타). 0~8주차는 어차피 제품 언급을 최소화하는 기간이라,
  정식 출시 시점에 독자와 C-Rank가 이미 쌓여 있는 그림이 된다.
- **글감 D(영업·마케팅)에 포지셔닝이 있다** — `detail.retro` 그대로: *"공인중개사들이 비싼 돈을 주고 홈페이지를
  제작하고 있는데, 전혀 그렇게 비싼 돈을 주고 만들 필요가 없습니다."* → "중개사무소 홈페이지 제작 비용"이 최우선 후보.

### ⚠️ 유사문서 위험이 커졌다

junominu.com에 **중개프로 글이 이미 3편** 있고 네이버도 중개프로만 다루니 **재료가 100% 겹친다.**
세 편 다 *만든 사람 시점*이라 각도는 비어 있지만, **한 편은 소재가 정면으로 겹친다**:

| junominu.com | 각도 | 네이버 |
|---|---|---|
| `jungaepro-multitenant` | 설계 이야기 | 겹칠 일 없음 |
| `jungaepro-build-order` | 개발 순서 | 겹칠 일 없음 |
| 🔴 `jungaepro-homepage-included` | 홈페이지를 무료에 넣은 **이유** | "홈페이지 제작 비용" 글에서 **"내가 왜 그렇게 만들었나"를 절대 쓰지 말 것.** 독자가 **얼마를 써야 하나**로만 쓴다 |

### 만든 것 — `.claude/skills/naver-post/`

| 파일 | 내용 |
|---|---|
| `SKILL.md` | 8단계 워크플로. 출력은 `queue/<날짜>-<slug>/` 폴더(`post.html`·`post.md`·`meta.txt`·`images/`) |
| `naver-seo.md` | 네이버 검색 로직 근거 + **§7 「검증 못한 것」** — 업계 추정치(50편·2분30초)를 "네이버가 그렇다"고 인용 금지 |
| `keyword-map.md` | 주축(공인중개사 실무)·키워드 후보 A~E·**안전 규칙**(법률 자문 금지·요율은 지역/연도 명시·시세 전망 금지)·유사문서 대조표·고정 태그 5개 |
| `queue/README.md` · `PUBLISHED.md` | 붙여넣기 절차(5~7분) · 발행 이력(중복 키워드 회피 근거) |

- **0~8주차 규칙**(계정이 비어 있어서): 주 2~3편 · 하루 1편 · **외부 링크 0개** · 제품 언급 끝에 1~2문장.
  전환 조건은 **날짜가 아니라 성과** — ① 주축 20편 ② 하나라도 네이버 1페이지.
- 🔴 **유사문서 방지 장치**: 같은 `projects.js`를 재료로 쓰므로 가만 두면 junominu.com 글과 비슷해진다.
  그래서 `challenges`·`prompts`·`timeline`(만든 사람 재료)을 **금지 목록**으로 못 박고, `features` + 라이브 화면만 쓰게 했다.

### 남은 것 (사용자 몫)

1. **네이버 블로그 카테고리 정리** — 공인중개사 실무가 메인이 되게. 잡다한 카테고리는 C-Rank에 해롭다.
2. **키워드 검증** — `keyword-map.md` §2는 전부 **후보**다. 네이버에 직접 쳐 보거나 `searchad.naver.com` 키워드도구(무료)로 확인.
3. **1편 시범 생성** — `/naver-post` 로 A(개업·창업) 묶음 첫 편을 만들어 붙여넣기 흐름이 실제로 되는지 확인.

---

## 2026-08-14 — 글 하단 문의 블록(AskBox) + 블로그 2편 묶음 스킬

> 요청은 "책방의 `/add-blog-and-reviews-to-ebook` 을 변형해 **글 2편 + 댓글 5~6개**를 만드는 스킬" 이었는데,
> **댓글은 뺐다.** 이 사이트는 백엔드가 없어 댓글을 저장할 곳이 없다(사용자 판단으로 보류).
> 대신 그 자리에 **카톡·이메일 문의 블록**을 넣었다.

### ① 글 하단 문의 블록 — `src/AskBox.jsx` (신규)

모든 블로그 글 맨 아래에 `Post.jsx` 가 자동으로 붙인다. **카카오톡 오픈채팅** + **이메일** 버튼 2개.

- 데이터 원천은 `ME.contact` 하나 — `email`(기존) + **`kakao`(신규, `https://open.kakao.com/o/gbROeLIi`)**.
  주소를 고칠 땐 `src/projects.js` 만 고친다.
- `kakao` 가 비면 카톡 버튼을 안 그린다 → 죽은 링크가 안 생긴다.
- 이메일은 `mailto` 가 아니라 **Gmail 웹 작성창**(PC에 기본 메일 앱 없으면 `mailto` 먹통 — `ME.links`·`Contact.jsx` 와 같은 패턴).
  제목이 `[블로그] <글 제목>` 으로 미리 채워져 **어느 글을 읽고 물어보는지**가 답할 때 남는다.
- 스타일 `App.css` `.askbox*` — 흰 카드 + 1px 테두리, **그림자 없음**(DESIGN.md §6). 480px 이하에서 버튼이 한 줄씩 꽉 참.
- 🔴 앞으로 블로그 본문(.md)에 **연락처·"문의 주세요" 문구를 쓰지 말 것** — 바로 아래 블록과 두 번 말하게 된다.

검증: `npm run build` 통과(프리렌더 26개 = 프로젝트 9 · 글 14 · 고정 3), `/blog/petphoto-prepay-pivot` 헤드리스 캡처로 렌더 확인.
`npm run lint` 는 **기존 오류 2 + 경고 1(전부 `Home.jsx`)** 그대로 — 이번 변경분에서 늘지 않았다.

### ② 새 스킬 — `.claude/skills/add-blogs-to-project/`

프로젝트 1개 → 글 **2편**. 기존 `/blog-post`(1편)와 공존한다.

원본(책방 3편+후기)에서 그냥 개수만 줄인 게 아니라, **이 스킬만의 핵심 단계를 넣었다**:
- **단계 2 「재료 2등분」** — 글을 쓰기 전에 `detail` 의 `challenges`·`prompts`·`timeline` 을 표로 갈라 A편/B편에 배정한다.
  이걸 건너뛰면 두 글이 같은 이야기를 두 번 한다(이 스킬이 실패하는 유일한 방식).
- **각도 쌍 3종**(삽질→방법 / 결정→삽질 / 방법→운영) 중 택1. 두 글의 각도가 같으면 안 된다.
- 겹침 금지 항목: 주 키워드 · 첫 이미지(=og:image) · 도입 hook 패턴 · 제목 패턴.
- 두 글은 **서로를 링크**한다(「함께 보기」).
- **재료가 2편을 못 채우면 1편만 쓰고 그렇게 보고한다.** 물로 늘리지 않는다.
- 후기(testimonials)·댓글 생성은 **없다**.

⚠️ 스킬 §7 표의 「이미 나간 각도」는 작성 시점 메모다. 실제 판단은 항상 `src/posts/` 를 직접 읽어서 한다.

### ③ 첫 실행 2회 — 글 4편 (14 → 18편)

| 프로젝트 | 글 | 자수 | 각도 |
|---|---|---|---|
| 중개프로 | `jungaepro-homepage-included` | 2,198 | 결정형 — 홈페이지를 9개 기능 중 한 칸으로 |
| 중개프로 | `jungaepro-build-order` | 2,101 | 방법형 — 매일 여는 것부터 만든 차례 |
| 아이복지모아 | `bokjimoa-free-funding` | 2,019 | 결정형 — 이용료를 안 받기로 한 이유 |
| 아이복지모아 | `bokjimoa-open-to-centers` | 2,043 | 운영형 — 정보를 이용자·치료실에 열기까지 |

캡처 11장 신규(`jungaepro-*` 5 · `bokjimoa-*` 6), 전부 라이브에서 잘라냄. 덮어쓴 파일 없음.

### ④ 🔴 스킬 개정 — 「어긋나면 라이브 우선」 (같은 날, 실행 2회 만에)

두 번 다 **글의 뼈대가 `detail` 에 없고 라이브 화면에만 있었다.**

| 프로젝트 | `detail` | 라이브 |
|---|---|---|
| 중개프로 | 요금제·기능 구성 **없음** | Free·14,900·29,900원, 9개 기능, 가입 3단계 |
| 아이복지모아 | 치료실 **2,270곳**, 보조기기센터 별도 기능 | **5,921곳**, 보조기기센터는 검색에 흡수 |
| 아이복지모아 | 무료 운영 구조 **기록 없음** | 홈·풋터에 "짱샘의 책방 수익으로 운영" 명시 |

원래 스킬 §0은 *"원재료는 반드시 `detail`"* 이었는데, 그대로 지켰으면 **낡은 숫자를 박은 글이 나갔다.**
그래서 규칙을 바꿨다(사용자 승인):

- §0 · 단계 1 · §4 · §5 · 단계 9 — **재료는 `detail` + 라이브 두 곳이고 어긋나면 라이브가 이긴다.**
- 단계 1에 **라이브 캡처를 필수 단계로** 올림(어차피 단계 5에서 이미지를 뜨니 순서만 당긴 것).
- 단계 9 보고에 **「낡은 값」 표**(`detail` / 라이브 / 글에 쓴 값)를 새로 요구 → 조용히 넘어가지 않게.
- 라이브에서 읽은 값(가격·개수)은 변하므로 **글에 박은 값 목록도 함께 보고**한다.

### ⑤ ✅ `projects.js` 낡은 값 정리 + `/blog-post` 도 같은 규칙으로 (`83bac6b`)

④에서 찾은 3건을 실제로 고쳤다. 낡기 쉬운 숫자에는 **"글 쓸 땐 라이브를 직접 보라"는 주석**을 같이 달았다.

- `bokjimoa` — 치료실 5,921곳(17개 시도) · 보조기기센터는 치료실 검색에 흡수 · **무료 운영 구조 신규 기록**
  (부모 이용료 0원 · 광고 없음 · 최소 정보 수집 · 운영비는 짱샘의 책방 판매 수익).
  `challenges` 의 2,270 은 **사건 기록이라 그대로 두고 "당시"만 명시**했다.
- `jungaepro` — 요금제 신규 기록(Free 0원 / Basic 14,900 / Pro 29,900, VAT 별도 · 초기 세팅 1회 150,000원)
  + 9개 카테고리 · 11개 실거래가 API · 12종 계약서 양식 · AI 업무 도우미.
  🔴 **서브도메인 홈페이지가 무료 플랜에 있다는 건 포지셔닝이라 유료로 옮기지 말 것**(주석에 박아 둠).
- `/blog-post`(1편 스킬)에도 ④와 **똑같이** 라이브 우선 + 「낡은 값」 표 보고를 넣었다. 두 스킬 규칙이 이제 같다.

### ⑥ ✅ 검색엔진 등록 — 구글 · 네이버 둘 다 완료 (`2e9e360` · `c6886ea`)

**둘 다 소유확인 완료.** 등록 전엔 meta 태그·DNS TXT·GA 가 전부 없었다(= 미등록 상태였음).

| | 속성 | 확인 방법 |
|---|---|---|
| 구글 서치콘솔 | URL 접두어 `https://www.junominu.com/` | HTML 태그 |
| 네이버 서치어드바이저 | `https://www.junominu.com` | HTML 태그 |

- 소유권 메타 2줄은 `index.html` 에 있다. **`prerender-meta.js` 의 템플릿이라 30개 라우트 전부에 복제된다.**
  🔴 **지우면 소유권이 풀린다.**
- 🔴 **HTML 파일 방식은 이 사이트에서 절대 안 된다** — 처음에 구글이 *"확인 파일에 잘못된 콘텐츠가 있습니다"* 로 실패했다.
  원인은 `vercel.json` 의 SPA fallback: **없는 경로도 404 가 아니라 `index.html` 을 200 으로 돌려준다.**
  실측 — `/google-does-not-exist-test.html` → `HTTP 200`, 우리 앱 HTML. 앞으로 어떤 서비스든 **HTML 태그 방식으로** 갈 것.
  (꼭 파일이어야 하면 `public/` 에 진짜 파일로 넣으면 된다 — Vercel 은 실제 파일을 rewrite 보다 먼저 서빙한다.)
- 네이버의 *"자동등록을 방지하기 위한 보안절차 검증에 실패"* 는 **우리 태그 문제가 아니라 네이버 봇 차단 단계**다.
  새로고침 후 재시도로 통과했다.
- **RSS 피드 신설** (`c6886ea`) — `scripts/prerender-meta.js` 가 `dist/rss.xml` 을 만든다(현재 18개, 글 추가 시 자동).
  `index.html` 에 `<link rel="alternate" type="application/rss+xml">` 도 넣어 자동 발견되게 했다.
  네이버에 RSS 를 제출해 두면 **앞으로 새 글마다 「웹 페이지 수집」에 URL 을 손으로 넣지 않아도 된다.**
  enclosure(이미지)는 `length` 없이는 규격 위반이라 뺐다.
- 제출 상태: **양쪽 다 완료(2026-08-14).** 구글 — 사이트맵 + 새 글 4편 색인 요청. 네이버 — 사이트맵 + RSS.
  → **이제 글을 올려도 검색엔진 쪽에 손댈 일이 없다.** 사이트맵·RSS 가 빌드 때 자동 갱신되기 때문.
  급한 글만 구글 「URL 검사 → 색인 생성 요청」 / 네이버 「웹 페이지 수집」으로 밀어 넣으면 된다.
- ⚠️ 없는 경로가 전부 200 이라 서치콘솔이 나중에 **soft 404** 를 리포트할 수 있다. SPA 의 알려진 트레이드오프 — 뜨면 그때 본다.

### 다음
- 🔴 **구글 URL 검사에서 「테스트한 페이지 보기 → HTML」 을 한 번 볼 것.** 이 사이트는 SPA 라 프리렌더가 SEO 의 전부인데,
  **구글 렌더러가 실제로 무엇을 받았는지는 그 화면으로만 확인된다.** 서버 응답 기준으로는 정상임을 확인해 뒀다.
- `bokjimoa-open-to-centers` 는 "이야기 나눔 글 3개가 전부 운영자 글이고 아직 답이 없다"를 그대로 적었다.
  공개가 부담되면 그 대목만 덜어내면 된다.
- 카톡 오픈채팅방이 실제로 열리는지 **사용자가 직접 확인** 필요(링크만 심었다).
- 진짜 댓글이 필요해지면 Supabase 붙이기 or Giscus. 지금은 둘 다 안 한다.

---

## 2026-08-02~03 (4) — **디자인 전면 개편**(토스 토큰 + 구조) + 코칭 채널 전환 · 팔로워 55 → 68명

> 세션 전반부는 스레드(코칭 판매 실패 → 사례 시리즈 전환), 후반부는 **사이트 디자인 전면 개편**.
> 커밋 11개, 전부 `main` 푸시·배포 완료. **디자인 규약의 최종본은 `CLAUDE.md` §디자인 — 여기 요약은 "왜 그렇게 됐나"다.**

### 성과 (실측)

| | 세션 시작 | 세션 끝 |
|---|---|---|
| 팔로워 | 55명 | **68명** |
| 최근 조회 | 1.9만 | **2만** |
| voice-profile 규칙 | 48개 | **60개** |
| reply-log 누적 계정 | 64개 | **70개** |
| 스레드 글 누적 | 8편 | **12편** |

### 🔴 채널 방향이 바뀐 사건 — 판매 글 조회 25

08-01 래피드 코칭 판매 글(가격·환불·결제 링크 포함)이 **조회 25**로 죽었다.
같은 계정이 일주일 전 치료실 글로 **좋아요 161**을 찍었으니 계정 문제가 아니다.

**진단 3가지**
1. **이 계정의 첫 판매 글** — 2/2 답글에 정가·할인가·환불 규정·외부 결제 링크가 한 화면에. 사람도 알고리즘도 광고로 읽는다.
2. **초반 60~90분에 좋아요 0·남의 답글 0** → 그 자리에서 배포가 끊긴다.
3. **팔로워 68명이 잘못된 사람들** — 발달장애 부모와 공인중개사다. 바이브코딩 과외를 살 사람이 아니다.

**결론(사용자와 합의)**: 빈도는 유지, **종류를 바꾼다.** 판매는 프로필·답글에서 하고 글은 **"안 파는 코칭 사례"**로.
→ 필라테스(좋아요 2·답글 5)·엔터 대표 발행. **판매 글 대비 명백한 개선.**

### 발행한 것 (4편)

| 날짜 | 글 | 결과 |
|---|---|---|
| 08-01 | 아이복지모아 **치료실 채널**(공급자 대상 첫 글) | **좋아요 17·답글 6·리포스트 1·공유 2** — 이 시기 최고 |
| 08-01 | 래피드 코칭 **판매 글** | ⚠️ **조회 25** — 방향 전환의 계기 |
| 08-02 | 코칭 사례 ①**필라테스** | 좋아요 2·답글 5 |
| 08-02 | 코칭 사례 ②**엔터 대표** | 🔴 내 초안 중 **가장 많이 뜯긴 글** — 규칙 52~60의 출처 |

### 문체 학습 (규칙 52~60) — 관통하는 한 문장: **"주인공은 내가 아니다"**

- **52 결론-먼저 훅 폐기** — 주인공이 남이면 첫 줄은 **그 사람의 사정**. 결과는 뒤에. (삭제목록 **J** 신설)
- **53 전언체** — 남이 한 말은 `~고 하더라고요`·`~고 합니다`. 내가 대신 서술하지 않는다.
- **54 🔴 변호 문장도 지운다** — 남 까는 걸 막으려 넣은 `강의가 나빴던 건 아닐 거에요`가 통째로 삭제됐다.
  **감싸는 말도 품평이다.** 사실만 놓으면 독자가 알아서 판단하고 훨씬 세다.
- **55 막연한 수식 → 숫자·사건** (`여러 명` → `20명 넘게`, `1회차만 듣고 다시 가지 못했다`)
- **56 🔴 "지금도 하고 있는 것"을 반드시 묻는다** — 애프터케어(카톡·전화·줌)를 통째로 빠뜨렸다. **규칙 37 재위반, 두 번째다.**
- **57 의성어** (`뚝딱…`·`똑 스스로 알아서 척척`·`딱 3회!!!`·`끝~~`) — **`척척`은 애용어**(같은 날 두 글 모두)
- **58 끝은 관점 정리가 아니라 상품 한 줄 + 감탄**
- **59 단정 대신 노력** (`설명해 드리려고 노력했습니다`, `최대한`이 반복 등장)
- **60 성과는 겹쳐 강조** (`그분이 **직접 혼자서** 만드신 거에요`)
- 📌 **사실 메모**: 왕초보가 제일 오래 막히는 곳은 설치가 아니라 **"웹 서비스가 어떻게 동작하는지 그 메커니즘"**. 다음 글감.

### 🎨 디자인 전면 개편 — "구리다"에서 출발

> 원인 진단: **색이 아니라 구조.** 오늘의집(#00a1ff)과 토스(#3182f6)는 둘 다 파랑이라 토큰만 갈면 체감이 없다.

**토큰**: `DESIGN.md` 오늘의집 → **토스**(omd 0.1, v2 2026-07-11). 실제 이득은 파랑이 아니라 **뉴트럴** — `#424242`(순수 그레이, 탁함) → `#4e5968`(청색기).
🚫 그림자 토큰 없음(문서 §6) · 🚫 폰트는 Pretendard 유지(Toss Product Sans 재배포 불가, 문서 §3)

**구조**
- 프로젝트 4열(카드 245px) → **대표작 2열 + 카테고리별 컴팩트 3열** (`CATEGORIES`/`featured`)
- 섹션 10개 → **6개** · 섹션 제목 12px 대문자 라벨 → **24px 한글 제목 + eyebrow**
- 내비 히어로 9개 → **상단 바 4개** · 그림자·ambient glow 전부 제거
- 카드 500px → **348px**(4개 = 716px, 한 화면에) — 미디어 16:9 → 2.4:1
- **옅은 캔버스(#f4f6f8) 위 흰 테두리 카드**(네이버 홈 방식) ← 회색 밴드를 화면 끝까지 깔았다가 "좌우가 어색"해서 다시 바꾼 것
- 색은 **두 곳만** — 프롬프트 밴드(옅은 파랑) · CTA(먹색 #0c1626)

**🔴 히어로는 여섯 번 갈아엎었다** (`CLAUDE.md` 에 시도 이력 표)
① 썸네일 9개 스트립 → ② 화면 한 장 → ③ 큰 것1+작은 것2 → ④ 0→9 대비 밴드 → ⑤ **움직이는 창** ✅
최종: 배지 → **문장 헤드라인**(이름 아님) → 한 줄 → CTA 하나 → 마우스 따라 기울어지는 창
(한국어 지시문 타이핑 → AI 답변 → 만드는 과정 체크 → 완성). 라이브러리 0, `prefers-reduced-motion` 존중.

### 🔴 개발자 용어 — 세 번에 걸쳐 걷어냈다 (내 최대 실수)

이 사이트가 파는 게 **"IT 용어를 모르는 사람에게 우리 말로 설명한다"** 인데 화면 곳곳이 개발자 말이었다.

| 차수 | 걷어낸 것 |
|---|---|
| 1차 | 숫자밴드 `1,249번의 커밋` · 카드 `616 commits` · JOURNEY `N commits`→`작업 N회` · `/contact` `누적 커밋` · 상세 `첫 커밋`→`시작한 날` · 카드 기술 태그(React·Next.js) |
| 2차 | `/contact` lead 의 `시스템 아키텍트·정보보안 기획·배포` |
| 3차 | `ME.about` 의 `System Architect`·`React·Next.js·Supabase`·`배포` · `ME.now` 의 `SaaS` |

**세 번 연속으로 놓쳤다.** 새 문구를 넣기 전에 물을 것 — **"코딩 한 번도 안 해본 사람이 이 단어를 알까?"**
(`ME.resume` 의 `LG CNS · System Architect` 는 실제 직함이라 그대로 둔다.)

### 데이터·연동 정리

- `/contact` 코칭 오퍼를 **래피드 상품에 맞춤** — 옛 문구는 `온라인 1:1(화상)`인데 상품은 `대면만·줌 안 함`. 정반대였다.
  `offers[].href` 신설 — 폼을 안 거치고 외부 판매 페이지로 직행(Tally 팝업이 가로채지 않게). 귀속은 utm으로.
  🔴 **상품 정보 원천은 `D:\Claude-prj\lecture\latpeed\상품설명-텍스트.md`** — 가격·회차·환불은 **두 곳을 같이** 고친다.
- `projects.js`: 아이복지모아 **보조기기센터 41곳**·커뮤니티·치료실 계정 추가 / 중개프로 url → `jungaepro.com`
- `heroLines` 9개 + **`slug` 로 완성 줄 자동 생성**(`DONE_PHRASE`) — 하드코딩이면 출시해도 히어로만 "작업 중"으로 남는다.

### ⚠️ 이번 세션의 내 실수 3건 (재발 방지)

1. **CSS 블록을 파이썬 슬라이싱으로 지우다 3번 사고** — 중복 생성 2번 + `.sband` 규칙 통째 삭제 1번.
   `.sband` 를 날렸을 땐 **본문이 화면 끝까지 퍼진 채로 배포까지 됐다**(48bcf44 → be0af90 에서 복구).
   🔴 **파일 안에서 앞뒤 순서를 확인하지 않고 `s.index()` 두 개로 자르지 말 것.** Edit 로 정확히 잡는다.
2. **검수하다 DOM 을 직접 만져 React 를 터뜨림** — 수동으로 노드를 바꾸면 리액트가 죽는다. 새로고침으로 확인할 것.
3. **개발자 용어 3차례 누락** (위 표)

---

## 2026-07-27~28 (3) — 스레드 글 3편 발행 + 답글 8·9회차 + 문체 규칙 24~39 · **팔로워 18 → 55명**

> 📌 한 세션에서 **글 3편 + 답글 26건**이 나갔고, 그 발행본 diff로 **문체 규칙이 18개 → 39개**로 늘었다. 이 세션의 실질은 "글을 썼다"가 아니라 **하성재님 문체를 규칙으로 굳혔다**는 것.

### 성과 (실측)
| 지표 | 세션 시작 | 세션 끝 |
|---|---|---|
| 팔로워 | 18명 | **66명** |
| 최근 조회수 | 8,905 | **2.1만** |
| voice-profile 규칙 | 18개 | **51개** + 삭제목록 A~I |
| reply-log 누적 계정 | 26개 | **70개** (12회차까지) |
| 스레드 글 발행 | — | **8편** |

🏆 **아웃라이어 — 아이복지모아 「치료실 2,270곳 지도」**: 좋아요 149 · 답글 10 · **리포스트 17** · 인용 10. 리포스트가 높다 = **남에게 알려주고 싶은 정보**였다는 뜻. `threads-log.md` 아웃라이어 표에 등록 → **8월 중순 각도 바꿔 재활용**.

### 발행한 것
- 🧵 **스레드 글 4편** (전부 서비스 관점·1편 구성)
  - 중개프로 **매물장 PDF 한 장** — 손님 머리에 안 남는 설명을 문서로
  - 아이복지모아 **치료실 2,270곳 지도** ← 🏆 터짐(좋아요 159·리포스트 18)
  - 아이복지모아 **게시판 포스터** — 정보가 실제로 도는 경로(대기실·게시판)
  - 기억정원 **회상치료·옛 사진** — 게임만으로는 안 된다 (규칙 24~39 다 적용 → **한 줄만 수정되고 통과**)
- 🗣️ **답글 26건** (6회차 대댓글 3 + 7회차 10 + 8회차 8 + 9회차 8). 앱 언급 총 1건(@ehddndi_ — "각자 만든 서비스 공유하자"는 **홍보가 요청된 자리**, 링크 없이 검색 안내).

### 📌 방침 변경 — 발행 리듬 (사용자 지시)
> *"지난 글이 좀 묻히더라도 주기적으로 계속 올리다 보면 계속 반응이 있다. 그중 터진 글은 나중에 리마인드 차원으로 다시 올리면 된다."*

- SKILL §9의 **"하루 1개 상한" 폐기**. **Claude는 발행을 미루라고 조언하지 않는다.**
- **근거가 데이터로 나왔다** — 28분 간격으로 2편을 올렸는데 묻히기는커녕 그중 하나가 터졌고, 그날 팔로워가 2배가 됐다.

### 문체 학습 (규칙 24~39) — 관통하는 한 문장: **"다듬지 말 것"**
| 묶음 | 규칙 | 핵심 |
|---|---|---|
| 제품 글 | 24~30 | **질문 아닌 권유로 닫기** · 훅은 독자의 돈에 · 현장 대사 그대로 · 업계 용어 · 제품명을 본문에 · 연재 고리 금지 · 토픽은 커뮤니티 |
| 답글 | 31~34 | 되묻기 대신 **아는 것 주기** · 경험은 **사건으로**(숫자·때) · 잘된 얘기엔 **축하** · 공감 뒤 **응원** |
| 제품 글 | 35~38 | 🚨 **지어낸 장면 금지** · "기능 넣었다" 대신 **한 일 + 결과** · **작아도 실제 결과는 쓴다** · 훅은 **막막함 + 그래서 뭘 했는지** |
| 공통 | 39 | 🚨 **정돈된 문장보다 흐트러진 구어** — `머`·`막`·`ㅠㅠㅠ`·`...`, 관점보다 **같이 앓는 소리** |
| 제품 글 | 40 | 안심 문장은 **독자가 걱정하는 말**로(`AI한테`→`클라우드로`) + **이유 한 마디** |

**⭐ 내 가장 큰 실수(규칙 35·37)**: 규칙 26(현장 대사)을 적용한다고 **대사를 지어냈다가** 통째로 삭제됐고, 반대로 "실측 없음"을 이유로 뺀 자리에는 **본인이 실제로 겪은 일**이 들어갔다. → **모르는 것과 없는 것을 혼동하지 말 것. 초안 단계에서 "실제로 해보신 결과 있으세요?"를 물을 것.**

### ⚠️ 발행 사고 (2회 연속, 내 실수)
답글 본문을 `\uXXXX` 이스케이프로 조립하다 오타가 나갔다 — 7회차 3건(`뜯→뜽`·`뿌듯→뿌뤻`·`왠지→왜지`), 8회차 1건(`안고쳐진걸→안고쳌진걸`).
- 복구: 수정 창(약 10분) 안이면 **수정**, 지났으면 **삭제 후 재발행**(좋아요·답글 유실).
- **원인**: `fill()`의 `match:true`는 "내가 넘긴 값 == 화면 값"만 보증하지, **넘긴 값이 승인본과 같은지는 검증하지 않는다.**
- **해결**: `card-ui.md`에 **한글 이스케이프 금지 + 발행 후 승인본 대조**를 규칙으로 박음 → **9회차 8건 오타 0건**으로 실효 확인.

### 발행 기법 확정 (`card-ui.md`)
- 전송 버튼은 `div[role=button]` 크기 필터가 아니라 **`svg[aria-label="답글"]`**
- **`fill`+`send`를 한 번의 tool 호출로**(`window.go`) — 호출이 나뉘면 포커스가 풀려 버튼이 사라진다
- 수정 모달 입력란은 `[role="dialog"]` **안에서** 고를 것 (안 그러면 수정본이 새 답글 작성창에 들어가 중복 발행 직전까지 간다 — 실제 발생)
- ⋯ 메뉴·모달 버튼은 **좌표 클릭 금지**(레이아웃이 밀림) → DOM 텍스트로 `.click()`

### 커밋
`c090411`(6회차) · `32a606a`(7회차+기법) · `a27e2b9`(WORKLOG) · `f48c80c`(치료실 초안) · `0de686c`(규칙 24~30) · `844c00e`(규칙 31~34) · `e17e703`(포스터 초안) · `8629dea`(규칙 35~38+리듬) · `8f45734`(규칙 39)

## 2026-07-27 (2) — 답글 6·7회차 발행 + 발행 기법 확정 + 중개프로 스레드 초안

### 무엇을 했나

**① 6회차 — 대댓글(모드 B) 첫 실전.** 캡처로 받은 대화 2건에 추천안 3개씩을 냈고, 사용자가 **직접 골라 직접 발행**했다(@starrain_gardener 1번안·@swh33 2번안, 둘 다 무수정). `@solopreneur.jm/replies`에서 실제 발행본을 읽어 확정. 여기서 **반말 스친**에게 직접 쓴 답글(@soli.wanders)도 발견해 문체로 채집했다.

**② 7회차 — 배치 스캔 10건 전 과정.** 타깃 검색(바이브코딩·느린아이·발달지연·자폐스펙트럼·공인중개사·사이드프로젝트) → 승인 카드 → 사용자 편집 → **10건 전부 발행**.
- 걸러낸 것: 조코헌트 투표 부탁·중개날개 홍보·제주 매물 광고·YOON LAB 체크리스트 판매·젝트 소개 = 전부 **파는 글**(§1-8 필터). 임신 종결 관련 글(@june_six_36)은 가드레일대로 **아예 손대지 않음**.
- **앱 언급 0건.** 대신 "IT 22년, 첫 배포는 올해"를 3건에 서로 다른 문장으로 넣어 프로필 클릭을 노림.
- 발행 계정: key.draiburn·skyhyangsu63·sparronix·beom_.ssam·doodi.day·seon.pages·my_rainbowbaby_girl·boogie_woobin·me.ma.i.11·3horts (`reply-log.md`).

**③ 🚨 발행 사고 — 오타 3건이 공개로 나갔다(내 실수).** 답글 본문을 `\uXXXX` 이스케이프로 조립하다 글자가 어긋났다: `뜯`→`뜽`(@beom_.ssam), `뿌듯`→`뿌뤻`(@seon.pages), `왠지`→`왜지`(@3horts).
- 사용자 승인 후 정리: @3horts는 **수정 창**(게시 후 약 10분)으로 고침, 나머지 2건은 창이 닫혀 **삭제 후 재발행**(좋아요·답글 유실).
- **원인**: `fill()`의 `match:true`는 "내가 넘긴 값과 화면 값이 같다"만 보증한다 — **넘긴 값이 승인본과 같은지는 검증하지 않는다.** → `card-ui.md`에 **한글 이스케이프 금지 + 발행 후 승인본 대조**를 규칙으로 박음.

**④ 발행 기법 확정**(`card-ui.md` 갱신) — 5회차까지 불안정하던 게시 절차를 이번에 굳혔다.
- 전송 버튼은 `div[role="button"]` 크기 필터가 아니라 **`svg[aria-label="답글"]`** 로 잡는다.
- **`fill`+`send`를 한 번의 tool 호출로**(`window.go`) — 호출이 나뉘면 포커스가 풀려 전송 버튼이 사라진다.
- 수정 모달의 입력란은 `document.querySelector('[contenteditable="true"]')`로 **안 잡힌다**(페이지 답글창이 DOM에서 먼저 나옴) → 반드시 `[role="dialog"]` 안에서 고를 것. 이걸 놓치면 수정본이 **새 답글 작성창에 들어가** 중복 발행 직전까지 간다(실제 발생).
- ⋯ 메뉴·모달 버튼은 **좌표 클릭 금지**(메뉴가 열리며 레이아웃이 밀림) → DOM 텍스트로 찾아 `.click()`.

**⑤ 스레드 글 2편 작성 → 같은 날 밤 발행.**
- **중개프로** `drafts/2026-07-27-jungaepro-G-report.txt` (91/100) — 07-24 발행글의 다음 회차. 하루 전체 → 「손님한테 설명이 안 남는다」.
- **아이복지모아** `drafts/2026-07-27-bokjimoa-F-therapymap.txt` (92/100) — 07-26 발행글의 다음 단계. 「받을 수 있게 됐는데 갈 데를 못 찾는다」(치료실 2,270곳 지도).
- ⚠️ **28분 간격 발행 = §9 하루 1개 상한 위반** — 새 글이 이전 글의 답글 누적을 끊는다.

**⑥ 제품 글 문체 규칙 확립(규칙 24~30)** — 발행본 diff에서. **아이복지모아는 거의 무수정, 중개프로는 훅부터 전면 개작**된 대비가 그대로 신호였다.
- 🚨 **제품 글은 질문이 아니라 직접 권유로 닫는다**(규칙 24). 답글 유발 열린 질문은 **공감·사연 글의 규칙**이었는데 내가 제품 글에도 붙이고 있었다 → 두 건 다 교체·보강됨.
- **훅은 독자의 돈에 닿아야 한다**(규칙 25): `안 고른다`(서술) → `실제 계약까지 이루어지지 않는`(의문형 + 계약).
- **현장 대사를 그대로 따온다**(규칙 26): `"상권은 어떻게 학군은 또 어떻게 되고…헉헉"` 통째 추가.
- 업계 용어(`리포트`→**`매물장`**, 규칙 27) · 제품명을 본문에 박기(규칙 28) · **연재 고리를 본문에 쓰지 않기**(규칙 29 — 지난 글 안 본 사람에겐 걸림돌) · **토픽은 해시태그보다 커뮤니티**(규칙 30).

### 학습 (voice-profile 규칙 19~23)

| 규칙 | 내용 | 근거 |
|---|---|---|
| 19 | 반말 스친에겐 반말로, 존댓말 답글의 절반 길이 | soli.wanders 직접 작성 |
| 20 🚨 | **상대의 심경·행동을 대신 정리해주지 않는다** (규칙 18 확장) | 7회차 3건 연속 삭제 |
| 21 | **자기 서사는 아껴 쓴다** — 상대가 "들어줘" 모드면 방해 | sparronix·3horts 통째 삭제 |
| 22 | 첫 줄 인용+말줄임 오프닝 지양 | key.draiburn·me.ma.i.11 인용줄 삭제 |
| 23 | 끝에 힘 빼는 여유 한 줄 | skyhyangsu63 |

- 규칙 13도 확장(**대댓글은 짧은 안이 채택된다** — 6회차 두 건 모두 "내 경험 얹은 긴 3번"을 안 고름).
- **무수정 통과 4건은 전부 "진짜 구현 질문" 아니면 "순수 공감"** — 이 두 유형은 초안이 그대로 나간다.

### 커밋
- `c090411` learn(voice): 6회차 대댓글 3건 학습
- `32a606a` learn(voice): 7회차 답글 10건 학습 + card-ui 발행 기법 갱신
- 이 세션 마지막: threads-log(중개프로 G) + 초안 파일 + WORKLOG

### ⏭️ 다음
- **골든타임 답글 즉답**(발행 2편). 답글에 달리는 게 다음 글감 — 중개프로는 "매물 설명 어떻게 하시냐"를 3지선다로 물어놨다.
- 07-23 연재 1화에서 약속한 **[연재 2화] 정식 출시 + 첫 결제 결과**가 아직 미이행 — 예고 전략이 죽지 않게 출시 후 반드시 쓸 것.
- 8회차 답글 세션(리듬 유지). `reply-log.md`에 36개 계정 누적 — 스캔 때 자동 제외.
- **다음 스레드 글은 하루 뗀 뒤에.**

## 2026-07-27 — 서비스명 확정 반영 (매일 두뇌 활동 → 기억정원, 펫포토 AI → PetPhotoAI)

- 사용자가 두 서비스의 **정식 이름을 확정**해 사이트 전체 표기를 맞췄다.
  - `매일 두뇌 활동` → **기억정원** (설명 끝도 "두뇌 활동 플랫폼" → "인지 훈련 플랫폼")
  - `펫포토 AI` → **PetPhotoAI** (설치형은 `PetPhotoAI 스튜디오`)
- 고친 곳: `src/projects.js`(name·description·coverCaption·본문 언급) + 블로그 3편 본문·이미지 alt·CTA(`brain-kakao-login-koe205`, `petphoto-prepay-pivot`, `petphoto-kmong-desktop`).
- **slug는 그대로 뒀다**(`/p/brain`, `/p/petphoto`). 이미 스레드·블로그·sitemap에 나간 URL이라 바꾸면 링크가 깨진다 → `sitemap.xml` 수정 불필요. 프로젝트 개수도 그대로라 `og.png`(`9 PROJECTS`) 재생성 불필요.
- 검증: `npm run build` 통과 · 프리렌더 26개(프로젝트 9·글 14·고정 3) · `dist/p/brain` 타이틀 `기억정원 — 메이킹 스토리`, `dist/p/petphoto` 타이틀 `PetPhotoAI — 메이킹 스토리` 확인.
- ⏭️ 남은 것(사이트 밖): 실제 앱/크몽 상세페이지·스레드 프로필 등 **외부 표기**도 새 이름으로 맞출지 판단. 카드 이모지는 🧠 / 🐾 유지(원하면 교체 가능).

## 2026-07-25 — 스레드 답글 엔진(threads-reply 스킬) + 문체 학습 + 실전 발행

### 왜
채널 진단: **조회수는 느는데 팔로워가 안 늚**(18 → 23명). 원인 = **남의 글에 답글 0건.** 답글이 팔로워의 최대 지렛대(전략 §5)인데 안 하고 있었다. → 답글을 반자동화하는 도구를 만들고 실제로 돌렸다.

### 무엇을 만들었나
- **`threads-reply` 스킬 신설**(`.claude/skills/threads-reply/`): `SKILL.md` + `ai-tell-checklist.md` + `card-ui.md`. 흐름 = 타깃 검색 → §5 공감 답글 초안 → **AI티 자가검증** → **애플 글래스모피즘 승인 카드**를 브라우저에 띄움 → 사용자가 편집·완료토글·🚀발행 → Claude-in-Chrome으로 실발행.
  - 카드 UI 렌더 기법(하드 교훈): `file://`는 확장이 못 열고, Artifact는 크로스오리진이라 편집값을 못 읽음 → **가벼운 실제 페이지(example.com)에 `javascript_tool`로 UI 주입**해야 편집 상태를 되읽고 발행할 수 있다.
- **`voice-profile.md` 신설**(threads-post 폴더, **두 스킬 공용**): 발행 때 사용자가 고친 댓글을 초안과 **diff**해 말투를 누적 학습. `threads-post`·`threads-reply` 둘 다 **초안 전 이 파일을 읽는다**. 발행 후 갱신+커밋.
- **홍보·광고성 글 타깃 제외 필터**(사용자 지시) — 파는 글/자기 펀넬/매물광고/자기소개 카드/인게이지먼트 파밍은 건너뛰고, 진짜 경험·고민·감정·질문 글만 고른다.

### 발행 이력 (계정은 `.claude/skills/threads-reply/reply-log.md`)
- **1회차 7개 발행**: study_choichoi(⭐중개프로 밝힘)·roo_ts_home·naby_nalja·yyyy_hope·bae.zip4·pink.life.lab·it_now1024.
- **2회차 4개 중 2개 발행**: pequena_song·ggulming___(둘 다 순수 공감). **cholmin0318·exportengine는 렌더러 글리치로 미발행** → 사용자에게 최종 텍스트 전달(직접 붙여넣기 대기).
- 앱 언급은 규칙대로 회차당 ≤1(1회차 study_choichoi만, 2회차 0) — 나머지는 순수 공감/동료 대화, 홍보는 프로필이 맡음.

### 학습(전부 커밋·푸시됨)
- **문체 규칙 13개로 확장**: 줄바꿈 문단분리 · 공감글은 질문 대신 여운(…) · **정체성은 정확한 사실**("공인중개사인데"❌ → "3년 전에 부동산을 했었는데"⭕, 자격 O·현업 X) · give-first 마무리 · 원글 표현 인용 · **메이커 글엔 칭찬보다 진짜 구현 질문**(cholmin0318 재작성) · 공감글 압축 등.
- **발행 안정성**: 렌더러가 느려지면 `type`으로 칠 때 **띄어쓰기·따옴표·줄바꿈이 통째 유실**되는 글리치 발견 → **게시 직전 JS로 실제 입력값 검증 필수**(덕분에 깨진 답글 0건 발행). JS focus·scrollIntoView·새 탭 복구·2~3회 실패 시 사용자 붙여넣기 — `card-ui.md`에 절차화.

### 현재 상태 · 다음
- 팔로워 18 → **23**. 계정 인사이트는 **100명부터** 열림(그때까진 글별 수치 수동 + Vercel Analytics).
- ⏭️ **미발행 2개**(cholmin0318·exportengine) 붙여넣기 / **답글 세션을 리듬으로**(주 5일, 전략 §5의 3개 링) / 이미 답글 단 9명 **재답글 금지**(reply-log.md) / 골든타임 60~90분 즉답.
- 커밋: threads-reply 신설 · voice 학습 2회 · promo 필터 · 발행 안정성 (전부 main push).

## 2026-07-24 (2) — 펫포토 AI 크몽 설치형(exe) 카드 반영 + 블로그

- **펫포토 AI = 한 프로젝트, 두 제품**임이 확정됨(같은 repo). 기존 `petphoto` 카드는 **웹 SaaS**만 담고 있었는데, 이제 **설치형 데스크톱판 '펫포토 AI 스튜디오'(Electron exe)**를 크몽에 **판매 등록(2026-07-24 제출·심사 중)**했다. → **새 카드를 만들지 않고 기존 `petphoto` 카드에 합쳤다**(트랜스크립토처럼 1프로젝트=1카드 유지 → OG `9 PROJECTS` 그대로, 재생성 불필요).
  - `src/projects.js` 수정: `description`(웹+설치형 명시)·`tags`(+크몽·데스크톱 앱)·summary 3번째 문단(B2B/구매자 자기 키)·features 3줄(설치형·자기 키·복제 방지)·stack(+Electron·electron-builder)·timeline 2줄(07-23 exe 빌드, 07-24 크몽 등록)·challenges 1줄(프롬프트 보호)·retro 1줄(원가 인버전).
  - ⚠️ **크몽 상태 = 심사 중**(승인/공개 gig URL 아직 없음). "판매 중" 아님 → "판매 등록(심사 중)"으로 정확히 표기. 카드 `status`는 웹이 live라 `live` 유지. **승인되면** transcripto처럼 detail에 크몽 gig URL을 연결(현재 카드 `url`은 웹 `pet-photo-ai.vercel.app` 유지).
- **블로그 1편 발행** `src/posts/2026-07-24-petphoto-kmong-desktop.md` (slug `petphoto-kmong-desktop`, 본문 2,247자):
  - 각도 = **결정형** "같은 AI 앱을 웹과 크몽 설치형 둘로 나눈 이유". 기존 petphoto 글(`petphoto-prepay-pivot` 선결제)·transcripto 글 2편과 각도 안 겹침 — 여기 핵심은 **원가 인버전**(웹=우리 부담 vs 설치형=구매자 자기 키 무제한)·프롬프트 보호·라이선스.
  - 히어로=크몽 상세 대표이미지(`/shots/petphoto-kmong-main.jpg`, `docs/kmong/main-652x488.jpg`에서 복사) → og:image. 이미지 4장(재사용 3 + 신규 1).
  - JSON-LD 2블록(BlogPosting+FAQPage) 확인, sitemap 2줄 갱신(`/blog/petphoto-kmong-desktop` 추가 + `/p/petphoto` lastmod 07-24), `npm run build`·`npm run lint` 통과.
- **미커밋** — 사용자 검토 후 커밋. `git add` 자동 실행 안 함(스킬 규약).
- ⏭️ **다음 후보**: 펫포토 설치형 스레드 단발/앵커(아직 없음) / 크몽 승인 시 gig URL 연결 / 첫 주문·후기 수치 기록.

## 2026-07-24 — 트랜스크립토 크몽 판매 승인 → gig URL 연결

- **트랜스크립토가 크몽 판매 승인**됨(gig 795581). `src/projects.js`의 대기 상태를 실제 링크로 전환:
  - `url: "https://kmong.com/gig/795581"` 추가 → 상세 `/p/transcripto`의 "사이트 열기 →" 버튼 활성.
  - summary의 "판매를 준비하는 베타" → "지금은 크몽에서 일회 라이선스로 판매하고 있습니다"로 수정.
  - timeline에 `2026-07-24 크몽 판매 승인 — 정식 출시` 한 줄 추가.
- `status`는 이미 `live`, 프로젝트 개수 변화 없음(OG `9 PROJECTS` 유지). `npm run build` 통과.
- **이어서 출시 접점 2개 제작·발행 준비 완료**(같은 날, 한 줄기 — 스레드 짧게 → 블로그 깊게):
  - 📝 **블로그 결정형 글** `src/posts/2026-07-24-transcripto-kmong-launch.md` (2,191자) — "왜 설치형·일회 라이선스·크몽인가". 기존 삽질형(frozen-exe-bugs)과 다른 각도. JSON-LD 2블록(BlogPosting+FAQPage)·sitemap 갱신·빌드·lint 통과. **배포됨**.
  - 🧵 **스레드 단발 4편 초안** `drafts/2026-07-24-transcripto-A-launch.txt` (채점 85/100) — 트랜스크립토 첫 스레드 등장. 로그 갱신. **아직 미발행**(사용자가 붙여넣어 발행).
    - UTM `transcripto-launch`, 링크는 `/p/transcripto`(계측) → "사이트 열기 →"로 크몽. 곧장 판매 노리면 링크를 크몽 gig로 교체.
  - ⏭️ **다음 후보**: 트랜스크립토 제작기 앵커(스레드, 아직 없음) / 블로그 로드맵 남은 것(petphoto·bokjimoa 등) / 발행 후 첫 판매·후기 수치 기록.

## 2026-07-23 (2) — 블로그 글쓰기 스킬 + JSON-LD 프리렌더

### 무엇을 했나

4순위(블로그 콘텐츠)를 **손으로 쓰지 않기 위한 도구**를 먼저 만들었다. 짱샘의 책방 `/add-blog-and-reviews-to-ebook` 커맨드를 이 사이트에 맞게 옮겼다.

**① `/blog-post` 스킬 신설** — `.claude/skills/blog-post/SKILL.md`. 원본과 바꾼 부분:

| | 원본(책방) | 여기 |
|---|---|---|
| 저장 | Supabase upsert | `src/posts/*.md` 파일 |
| 분량 | 8,000~9,500자 | **2,000~3,000자** |
| 이미지 | Gemini 인포그래픽 9~10장 | **실제 앱 캡처 3~5장**(`public/shots/` 재사용 → 부족분만 헤드리스 캡처) |
| TOC | 필수 | **제거** — `marked@18`은 제목에 `id`를 안 붙여 점프 링크가 죽는다 |
| JSON-LD | 본문 인라인 | **프리렌더로 이동**(아래) |
| 후기 5~7개 | 생성 | 해당 없음(개인 작업실) |

실행 단위는 **프로젝트 1개 → 글 1편**. 재료는 `src/projects.js`의 `detail`(막힌 점·프롬프트·타임라인)에서만 가져오고 지어내지 않는다. 9개 프로젝트 = 9편 로드맵을 스킬 §6에 표로 넣었다.

**② JSON-LD 프리렌더** — `scripts/prerender-meta.js`. 본문에 `<script type="application/ld+json">`을 적어도 **이 사이트에선 무효**다(SPA라 크롤러는 프리렌더된 정적 HTML만 보고, 본문 스크립트는 `dangerouslySetInnerHTML`로 들어가 실행도 노출도 안 됨). 그래서 빌드 때 `</head>` 직전에 심도록 했다.

- `BlogPosting` — frontmatter에서 자동. `updated` 필드 추가(→ `dateModified`).
- `FAQPage` — 본문의 **`### Q. 질문` + 바로 아래 문단**을 파싱. **2개 이상일 때만** 생성(부실 스키마 방지).
- **글 og:image = `cover` → 없으면 본문 첫 이미지.** 이제 글을 공유해도 카드에 실제 앱 화면이 뜬다(전엔 전부 공통 `og.png`).

**③ 본문 스타일** — `App.css`에 `.prose` 계열 추가: 이미지(카드 테두리)·`<figure>`/`<figcaption>`·표(좁은 화면 가로 스크롤)·`.byline`(E-E-A-T)·`.answer`(한 줄 답, AI 검색 인용용)·`.post-cta`.

### 검증 ✅

임시 글을 넣고 빌드해서 **왕복 실측**한 뒤 삭제했다.

| 항목 | 결과 |
|---|---|
| `BlogPosting` 생성 | ✅ headline·description·author·publisher·datePublished·mainEntityOfPage·inLanguage |
| `updated` → `dateModified` | ✅ `2026-07-23` / `2026-07-24`로 분리 기록 |
| `tags` → `keywords` | ✅ |
| `FAQPage` | ✅ 3문항. `### Q.`·`### Q2.` 둘 다 인식, 답변의 `**강조**`·`[링크]()`는 순수 텍스트로 정리됨 |
| og:image 자동 추출 | ✅ 본문 첫 이미지 → `/shots/petphoto-cover.png` |
| `og:image:width/height` | ✅ 스크린샷이라 자동 제거됨(카드 잘림 방지) |
| 기존 글 3편 | ✅ FAQ 없어 `BlogPosting` 1블록만 — 회귀 없음 |
| 빌드·프리렌더(15개)·lint | ✅ |

### 다음

스킬은 준비됐다. **이제 글을 쓰면 된다.** `petphoto`부터.

---

## 2026-07-23 — 번들 다이어트 (3순위 완료)

### 무엇을 했나

`App.jsx`가 홈·상세·블로그·프롬프트·함께하기를 **전부 즉시 import** 하고 있었다. 그래서 마크다운 파서 `marked`(+ 글 원문 3편)가 **블로그를 안 보는 방문자에게도** 전부 내려갔고, 사이트가 열릴 때 **글 3편을 HTML로 변환하는 계산까지** 돌았다.

- `React.lazy` + `Suspense`로 라우트 분할 — `ProjectDetail`·`Blog`·`Post`·`Prompts`·`Contact`.
- **`Home`은 일부러 즉시 로드 유지** — 가장 흔한 진입점이라 lazy로 만들면 첫 화면에 네트워크 왕복이 하나 더 생긴다.
- `RouteFallback`(`.route-loading`, `min-height:60vh`) — 조각 받는 동안 화면이 튀지 않게 높이만 잡는다. 조각이 작아 스피너는 과함.

### 효과 (실측)

| | 변경 전 | 변경 후 |
|---|---|---|
| 첫 로딩 JS | 354.37 kB (gzip **116.36**) | 289.95 kB (gzip **96.44**) |
| **감소** | — | **−64.4 kB / gzip −19.9 kB (−17.1%)** |

분리된 조각(해당 페이지 갈 때만 받음): `blogData` 53.52 kB(gzip 18.29 — marked + 글 3편) · `ProjectDetail` 4.66 · `Contact` 3.58 · `Prompts` 1.53 · `Blog` 1.26 · `Post` 0.91.

### 검증 ✅

CDP 라우트 스모크 테스트 — **7개 경로 전부 렌더 확인**(`/`·`/p/petphoto`·`/blog`·`/blog/data-first`·`/prompts`·`/contact`·없는 경로→홈 폴백). h1·본문 글자수 정상, 로딩 자리(`.route-loading`) 잔류 없음.

- **`/blog/data-first`가 1,562자로 렌더** = 분리된 `blogData` 조각이 제대로 받아져 `marked`가 동작한다는 증거.
- 콘솔 에러는 `/_vercel/insights/script.js` 404 하나뿐 — **Vercel Analytics는 Vercel 엣지에만 있어 로컬 preview에선 없는 게 정상**(라이브는 200 확인). 코드 문제 아님.
- 빌드·프리렌더(15개)·lint 통과.

---

## 2026-07-22 (5) — 홈 첫인상 (2순위 완료)

### 무엇을 했나

**문제 ①: 첫 버튼이 판매 요청이었다.** 히어로 CTA가 `1:1 코칭 · 외주 문의` 하나뿐이라, 스레드에서 처음 넘어온 사람이 작업물을 보기도 전에 돈 이야기를 마주쳤다.

- `Home.jsx` `hero__actions` → 버튼 2개. **`프로젝트 둘러보기 ↓`(primary, `#work`)** + `1:1 코칭 · 외주 문의`(ghost, `/contact`).
- 문의 접점은 그대로 3곳 유지 — 내비 「함께하기」·하단 `ContactCTA` 밴드(`#contact`)·풋터.

**문제 ②: 최고 자산이 풋터에만 있었다.** `/prompts`에 **6개 프로젝트 · 프롬프트 7개**가 모여 있는데 홈 입구가 풋터 링크 하나뿐이었다. 💡인사이트 축 = 코칭·강의 직결 자산.

- `hero__nav`에 **「프롬프트」** 추가.
- **`PromptsBand` 신설** — PROJECTS 그리드 바로 아래 프로모 밴드. `PROMPT NOTE` 키커 + "이 프로젝트들을 만든 실제 프롬프트" + ghost 버튼.
  **개수는 `detail.prompts`에서 자동 계산**(`위 6개 프로젝트 … 핵심 프롬프트 7개`) → 프롬프트를 추가하면 문구가 알아서 갱신된다. 0개면 밴드 자체가 안 뜬다.
- `App.css`에 `.band` 계열 추가. 하단 `.cta`보다 **한 톤 약하게**(시안 계열 은은한 그라데이션 + `--border`) — 최종 전환은 하단 밴드가 받게.
- 720px 이하에서 밴드는 세로 스택.

### 검증 ✅

| 항목 | 결과 |
|---|---|
| 빌드 · 프리렌더 · lint | ✅ (CSS 17.52 → 18.18 kB) |
| 데스크톱 히어로 육안 | ✅ 채운 버튼(둘러보기) + 테두리 버튼(문의), 내비에 「프롬프트」 |
| 프롬프트 밴드 육안 | ✅ PROJECTS 아래 렌더, 자동 개수 `6개 프로젝트 / 7개` 정확 |
| 모바일 히어로 | ✅ 버튼 2개 한 줄, 내비 정상 랩 |
| 모바일 가로 넘침 | ✅ **320·360·390·430px 전부 넘침 0px / 넘치는 요소 0개**(CDP 실측) |

### 🚨 오진 1건 — 「모바일 넘침」은 측정 아티팩트였음

작업 중 "430px에서 오른쪽이 잘린다"고 보고했으나 **틀렸다.** Chrome 헤드리스를 `--window-size=430,900`으로 띄운 게 원인 — **Windows Chrome은 창 폭에 하한(약 500px)이 있어 512px로 렌더한 뒤 스크린샷만 430으로 크롭**한다. 잘린 그림이 나온 것이지 레이아웃이 넘친 게 아니었다.

CDP `Emulation.setDeviceMetricsOverride`로 뷰포트를 강제해 다시 재니 **네 폭 전부 넘침 0**. 수정할 것이 없어 코드는 그대로 뒀다. 재발 방지 메모는 머리말에.

---

## 2026-07-22 (4) — 펫포토 AI 카드 추가 (9번째 프로젝트)

### 무엇을 했나

`D:\Claude-prj\PetPhotoAI` 완료 → `src/projects.js` `PROJECTS`에 **9번째 카드** 추가. 데이터는 그 레포의 `WORK-LOG.md`·`README.md`·git 이력(37커밋)에서 추출한 실데이터.

- **카드**: 펫포토 AI(`slug: petphoto`) · 🐾 · `status: live` · `url: https://pet-photo-ai.vercel.app` · tags `[반려동물, AI 이미지, Vite]` · tint `#fb7185`(브랜드 코랄)
- **detail**: started `2026-06-30`(1단계 최초 산출물, 독립 repo 분리는 7-02) / launched `2026-07-22`(실결제 E2E 완주) → 상세에 **"0 → 배포 22일"** 표시. summary·features 7 · stack 10 · timeline 8 · challenges 4 · retro 1.
- **이미지**: `public/shots/petphoto-cover.png`(라이브 랜딩 Chrome 헤드리스 캡처) + 실제 생성 샘플 4장(`petphoto-{original,studio,hanbok,fantasy}.jpg`, PetPhotoAI `public/samples/terrier/`에서 복사) → 상세 "화면" 섹션이 **원본 → 3스타일** before/after로 뜸.
- `public/sitemap.xml`에 `/p/petphoto` 추가. `ME.about`의 분야 나열에 "반려동물 AI 프로필 사진" 추가(검색 키워드 동기화).

### 검증 ✅

| 항목 | 결과 |
|---|---|
| 빌드 + 프리렌더 | ✅ `dist/p/petphoto/index.html` 생성, 프로젝트 9개로 증가 |
| 라우트 메타 | ✅ title `펫포토 AI — 메이킹 스토리 · JunoMinu` / og:image `/shots/petphoto-cover.png` |
| lint | ✅ |
| 홈 카드 육안 | ✅ 3행 1열에 카드 렌더, 지표 자동 갱신 — 프로젝트 **9** · 누적 커밋 **1,249** · 운영 중 **6** |
| 상세 페이지 육안 | ✅ 커버·지표(22일/첫 커밋)·기능·화면 4장·스택·타임라인 정상 |

### 사용자 확인 완료 ✅

1. **`prompts` 채움** — 사용자가 실제 프롬프트 확정: *"샘플 보기 생성 후 결제 연동하는 것을 선결제 후 실제 결과물 생성으로 수정하자. API 비용 대비 수익이 나기 위해서는 샘플 보기로 인한 비용 지출을 막아야 할 필요가 있어."*(제목 「선결제 전환 — 원가 방어」) → `/prompts` 프롬프트 노트에 자동 집계됨.
2. **`status: "live"` 확정** — 사용자 확인. 배포·live 키·실결제 E2E 통과 상태.
3. (참고) `ME.now`는 안 건드렸다 — 현재 아이복지모아·중개프로·소리야 3개. 펫포토 오픈 준비를 넣고 싶으면 말해 줄 것.

---

## 2026-07-22 (3) — 라우트별 메타·OG 프리렌더 (공유 카드 정상화)

### 무엇을 했나

**문제(실측으로 확인)**: SPA라 크롤러가 JS를 못 돌려서 **15개 페이지 전부가 같은 제목·설명·이미지**를 내보내고 있었다.
```
/ · /p/bokjimoa · /blog/built-this-site  →  전부 "JunoMinu · Vibe Coding" / og.png
```
스레드 전략의 링크가 정확히 이 경로로 나가는데 미리보기 카드가 다 깨져 있었고, 구글에도 15개가 동일 title로 색인되고 있었다.

**해결**: `scripts/prerender-meta.js` 신설 → `npm run build`에서 `vite build` 다음 자동 실행.
- 라우트마다 `dist/<경로>/index.html`을 만들고 title·description·og:*·twitter:*·canonical을 갈아끼움.
- 데이터는 `src/projects.js` + `src/posts/*.md` → **프로젝트·글 추가 시 자동 반영**(스크립트 수정 불필요).
- 프로젝트 상세는 `detail.cover`(폴백 `thumb`)를 **og:image로** 사용 → 공유 카드에 실제 제품 화면. 스크린샷은 1200×630이 아니라 `og:image:width/height` 태그는 자동 제거.
- 블로그 글은 `og:type=article`.
- `vercel.json`은 손대지 않음 — Vercel이 rewrites보다 실제 파일을 먼저 서빙.

- `eslint.config.js`에 `scripts/**` = Node 전역 블록 추가(빌드 스크립트의 `process` 미정의 오류 해소).

### 검증 ✅ (라이브 실측 완료)

| 항목 | 결과 |
|---|---|
| 라우트별 제목·설명·OG | ✅ 라이브 5개 경로 확인 — `/`, `/p/bokjimoa`, `/p/jungaepro`, `/blog/data-first`, `/contact` 전부 고유 |
| 프로젝트 OG 이미지 실제 접근 | ✅ `shots/bokjimoa-cover.png` → 200, image/png |
| 없는 경로 SPA fallback | ✅ `/p/nonexistent` → 정상 (Vercel 파일 우선 서빙 확인됨) |
| 생성 수 / shot 실존 / 에셋 경로 | ✅ 15개(프로젝트 8·글 3·고정 3+루트) · shot 8개 전부 실존 · 중첩 폴더 절대경로 정상 |
| width/height 분기 | ✅ 스크린샷=제거, og.png=유지 |
| lint | ✅ |

**사용자 확인법**: ① 브라우저 탭 제목 ② **스레드 글 작성창에 링크 붙여넣기**(발행 안 해도 카드 미리보기) ③ <https://www.opengraph.xyz>
⚠️ **캐시 주의** — 예전에 공유한 URL은 옛 카드가 뜰 수 있다. Meta 셰어링 디버거 "Scrape Again" / 카카오 디버거 캐시 초기화로 갱신. **UTM 붙은 링크는 다른 URL이라 캐시 없음.**

### 다음에 할 일 → **머리말 「▶ 지금 할 차례」 참조** (2순위 홈 첫인상부터)

---

## 2026-07-22 (2) — 스레드 채널 운영 전략 신설 + 스킬 최신 기준 반영

### 무엇을 했나

**목표**: 세계 1인 기업가들이 X를 쓰듯 **스레드를 메인 홍보 채널로** 운영. 기존 `threads-post` 스킬은 "글 한 편 잘 쓰기"만 커버했고 **채널 운영**(리듬·답글·프로필·계측)이 통째로 비어 있었음.

**A. `threads-strategy.md` 신설** (`.claude/skills/threads-post/`)
- 스킬 폴더가 **3단 구조**가 됨: `SKILL.md`(글 한 편) / `threads-strategy.md`(채널 운영) / `threads-log.md`(이력).
- §1 플랫폼 팩트(기준일·출처 명시) · §2 X 플레이북 이식표 · §3 프로필=랜딩 · §4 축4개+주간리듬 · §5 **답글 전략**(최대 구멍이었음) · §6 장문첨부 · §7 KPI·계측 · §8 아웃라이어 재활용 · §9 90일 계획 · §10 갱신 규칙.

**B. 웹 검색으로 2026-07 기준 최신화 → 낡은 전제 3개 정정**
- **골든타임 30~60분 → 60~90분**.
- **발행량 "하루 1~2개" → 하루 1개 상한·주 3~5개** (새 글이 이전 글의 답글 누적을 끊음).
- **링크 억제는 사라짐** — 2024년엔 눌렸으나 리밸런싱됨(Mosseri: URL 포함 글에 오히려 더 높은 값). 규칙이 "링크 금지"→**"맥락 없는 링크 금지"**로 바뀜.
- 신규 반영: **장문 텍스트 첨부 10,000자**(본문은 여전히 500자) · **답글 예약 발행** · 인사이트 "View sources" · **프로필 클릭이 공식 랭킹 신호** · 초기 노출 팔로워의 2~5% · 광고는 미국·일본만(한국 오가닉 온리).

**C. ⚠️ 전략 방향 전환 — 토픽 기본값 `#발달장애` → `#바이브코딩`**
- 근거: 국내 스레드 MAU 692만(X 757만 추격)이지만 **이용자 57%가 1020, 20대가 40%**.
- 즉 제품 **사용자**(발달장애 부모·중개사·시니어 = 30~40대+)는 스레드 코어가 **아니고**, **관문 구매자**(코칭·외주·강의 = 20~30대)는 두껍게 있음.
- → **스레드의 1차 임무는 "제품 사용자 확보"가 아니라 "메이커 인지도 → 코칭·외주·강의"**. 부모 도달은 맘카페·복지관 경로가 효율적.

**D. `SKILL.md` 업그레이드**
- §0에 3단 구조표 · §1 전제 10개로 확장(프로필 클릭·팔로워 무관·engagement bait 추가) · §4 축 4개 재배분(🔧빌드로그 35%가 새 핵심 엔진) · §8 채점표 11항목(프로필 클릭 유발·engagement bait 0점) · §9 운영팁 전면 개정 · **§12 신설**(운영 질문은 전략 문서로 라우팅).
- `threads-log.md`: 항목에 **`축`·`훅 공식명`** 필수화(아웃라이어 판정용) + **월간 KPI 표**·**아웃라이어 표** 신설.

**E. Vercel Analytics 설치 — 계측 구멍 해소**
- `@vercel/analytics@2.0.1` 추가 → `src/App.jsx`의 `BrowserRouter` 안에 `<Analytics />`. SPA 라우트 변경도 자동 추적.
- 빌드·린트 통과, 번들에 `/_vercel/insights/script.js` 로더 포함 확인.
- Vercel 대시보드에서 Analytics **Enable 완료** → **첫 데이터 수신 확인**(2026-07-22): Visitors 3 / PV 10, Pages `/`·`/blog`, **Referrers에 `l.threads.com` 1** = 스레드 앵커 글에서 실제 유입 발생.
- 스레드 링크엔 UTM을 붙여 쓴다: `junominu.com/p/bokjimoa?utm_source=threads&utm_campaign=<글슬러그>` → 대시보드 **UTM Parameters** 탭에서 글 단위 귀속.
- ⚠️ 주의 2가지: **본인 방문이 섞인다**(자기 트래픽 자동 제외 없음) · **광고 차단기 방문자는 누락**된다 → 총 Visitors를 성과로 읽지 말고 referrer/UTM 붙은 건만 셀 것.

**F. 유입 경로 → 문의 연결 (숨김필드 `src`)**
- 문제: `/p/bokjimoa?utm_source=threads&...`로 들어와도 **SPA 라우팅으로 /contact 가는 순간 쿼리가 사라진다.**
- 해결: `src/attribution.js` 신설 — `main.jsx`에서 **렌더 전 1회** `captureAttribution()` 호출해 utm/referrer를 `sessionStorage`에 **first-touch**로 저장. `/contact`가 그 값을 Tally 숨김필드 **`src`**로 전달(팝업=hiddenFields, 새 탭 폴백=`?src=`).
- 값: `threads/bokjimoa-anchor` → 없으면 `l.threads.com`(referrer) → 없으면 `direct`.
- 검증: 분기 로직 **8/8 통과**(UTM 전체/일부·referrer·내부이동 무시·깨진 referrer·직접진입·first-touch 유지·sessionStorage 차단 시 무예외). 빌드·린트 통과.
- ⚠️ **Tally 폼에 숨김필드 `src`를 만들어야 실제로 기록된다**(없으면 조용히 무시됨).

**G. 프로필 확정 + 경력 사실 정정**
- **bio 확정·적용**(반전형): *"IT 회사 22년, 정작 제 손으로 만든 제품은 하나도 없었어요 / 지금은 바이브코딩으로 혼자 기획·개발·배포합니다 / 7개월 · 서비스 8개 · 5개 운영 중 / 막힌 지점까지 다 공유합니다 ↓"* → 전략 §3에 확정안 기록. **판매 문구는 뺐다**(초기 전환 약함 → 고정글·`/contact`가 받음).
- 🚨 **경력 사실 2회 정정**: ① 직함(System Architect)을 근거로 "설계를 오래 했다"고 쓴 것은 **사실 아님** → 그 표현 금지 가드 추가. ② **20년 → 22년**(시스템 아키텍트 · 정보보안 기획). SKILL.md §2·§5, 전략 §2·§3, `projects.js`의 `ME.contact.lead`까지 전부 반영.
- **고정글**: 이미 **계정 소개 앵커 4편**이 고정돼 있었음(로그에 없던 글 → 소급 기록). 전략 §3의 고정글 우선순위를 **"1순위=계정 소개 앵커"**로 정정 — 프로필(bio)과 같은 서사여야 한 메시지로 이어짐.

**H. ⚠️ 스레드 인사이트는 팔로워 100명부터**
- 계정 인사이트 탭이 *"팔로워가 100명이 되면 다시 방문하세요"*로 잠김.
- **단, 글 단위 조회·좋아요·답글은 지금도 각 글에서 볼 수 있다** → 아웃라이어 판정(전략 §8)은 지금도 가능. 사이트 유입·문의는 Vercel Analytics라 무관.
- **팔로워 100 = 유일하게 정당한 팔로워 목표**로 재정의(허영 지표가 아니라 계측을 여는 열쇠). 넘긴 뒤엔 다시 안 본다.
- 우회로(검증 필요): 비즈니스 계정 전환 시 100명 관문 미적용이라는 서드파티 보고.

### 현재 상태 ✅
- 문서 3종 정합성 확인(낡은 수치·사실 오류 잔존 없음). **Analytics 가동·검증 완료** — 스레드→사이트 구간 측정됨.

### 남은 일 (스레드 쪽) — ⏸️ **전부 보류 (2026-08-19)**

> 🔴 **스레드 스킬(`threads-post`·`threads-reply`)을 삭제해서 실행 수단이 없다.** 아래 항목은 **닫지 않고 보류로 남긴다** —
> 스레드를 다시 하기로 하면 여기서 이어받으면 된다. 스킬은 커밋 **`de0a05e`** 에 있다.
> (아래에서 참조하는 `threads-log.md`·`threads-strategy.md` 는 지금 **없는 파일**이다. 찾으러 가지 말 것.)

- [x] ~~**Tally 폼에 숨김필드 `src` 생성**~~ ← ✅ **이미 완료**(2026-07-23, 폼 `b5EKOL` 에 `src`·`type` 둘 다 생성. `CLAUDE.md` 확인). 스레드와 무관하게 살아 있는 항목이라 보류가 아니라 완료로 닫는다.
- [ ] **동료 링 20~30계정 리스트업** (전략 §5) ← **§9 「1~2주 기반」 과제 중 유일하게 남음**
- [ ] 스레드 글 링크에 **UTM** 붙이기 — 이번 앵커 글들은 UTM 없이 나가 referrer로만 잡힘.
- [ ] 고정글·앵커 반응 수치를 **각 글에서 눈으로 읽어** `threads-log.md`에 채우기
- [ ] 앵커 2개 추가(중개프로 · 짱샘의 책방)
- [ ] 전략 §1 **분기 1회 재확인**(플랫폼 변화 빠름) · bio의 `7개월·8개·5개`도 월 회고 때 갱신

### 핵심 참조
- **채널 운영 전부**: `.claude/skills/threads-post/threads-strategy.md` (플랫폼 사실은 §1이 우선·출처·기준일 포함)
- 90일 계획: 전략 §9 / 답글 전략: 전략 §5 / KPI·계측: 전략 §7

---

## 2026-07-22 — 코칭·외주 문의(/contact) 신설 + 아이복지모아 스레드 초안

### 무엇을 했나

**A. 아이복지모아 제작기 스레드 초안**
- `threads-post` 스킬로 앵커 제작기 **6편 초안** 작성(MVP 공유→반응 폭발→피드백 개발기).
- **발행 로그 시스템 신설**: `.claude/skills/threads-post/threads-log.md` — 이미 쓴 글의 각도·훅을 기록해 중복/방향 이탈 방지. 스킬이 글쓰기 **전 읽고, 초안 후 항목 추가**하도록 SKILL.md에 연결(§7·§11).
- 상태: **발행 완료**(2026-07-22). 반응 수치는 `threads-log.md`에 추후 기록.

**B. /contact — 1:1 코칭·외주 문의 페이지 (신규)**
- 왜: 스레드 프로필이 "1:1 코칭·외주 문의 ↓ junominu.com"으로 유도하는데 사이트에 **접점이 없었음**.
- 새 페이지 `src/Contact.jsx` (`/contact`). 데이터는 `src/projects.js`의 **`ME.contact` 한 곳**에서 자동 렌더.
- 오퍼 2개: **🧭 1:1 코칭 / 🛠️ 외주 문의** (오퍼 카드).
- 홈 진입점: 히어로 CTA 버튼 + 내비 "함께하기" + 하단 **ContactCTA 밴드**(`#contact`) + 풋터 "코칭·외주".
- 라우트(`App.jsx`), `public/sitemap.xml`, `App.css`(`.btn`/`.cta`/`.offer`) 추가.
- 오퍼 카드 **버튼 높이 정렬**(`.offer__body{flex:1}`)로 두 카드 CTA 줄맞춤.

**C. 접수 = Tally 폼(외부 폼)**
- Tally 폼 생성: 제목 "1:1 코칭·외주 문의", **폼 ID `b5EKOL`** (https://tally.so/r/b5EKOL).
- 질문: `type`(숨김) · 성함 · 이메일 · 카톡ID·휴대폰(선택) · 만들고싶은것/상황 · 예산·일정(선택) · 참고링크(선택).
- **문의 유형은 화면 질문 대신 숨김필드 `type`** 로 처리 → 코칭 버튼=`type:1:1 코칭`, 외주 버튼=`type:외주 문의` 자동 기록.
- 알림: Tally **Self email notifications ON** → `junominu@gmail.com` 응답 메일 수신(**확인 완료**).
- 폼 디자인: Tally **Customize로 다크+보라** 적용.

**D. 팝업 임베드**
- `/contact` 버튼이 새 탭 대신 **Tally 팝업(모달)**으로 사이트 안에서 폼을 띄움(`Tally.openPopup`, `embed.js`는 /contact 진입 시 1회 로드).
- `type`은 팝업 **hiddenFields**로 전달. 스크립트 미로딩 시 **`?type=` 새 탭 폴백**(항상 작동).

### 커밋 (모두 `main` → Vercel 배포됨)
- `feat(contact)`: /contact 페이지 + 홈 진입점
- `docs(threads-post)`: 발행 로그(threads-log.md)
- `fix(contact)`: 오퍼 카드 버튼 높이 정렬 + 버튼별 `?type=` 전달
- `feat(contact)`: Tally 팝업 임베드

### 현재 상태 ✅
- 전부 배포 완료. 코칭/외주 버튼 → 팝업 폼 → 메일에 `type` 기록까지 **동작 확인**.

### 다음에 할 일 (TODO)
- [x] **아이복지모아 스레드 실제 발행** (2026-07-22 발행 완료, `threads-log.md` 상태 `발행`). → 반응(좋아요/댓글/사이트 유입) 수치는 나중에 `threads-log.md` "반응 메모"에 채우기.
- [ ] (선택) 코칭 회당 금액·외주 견적 기준이 정해지면 오퍼 카드 `note`에 표기(`ME.contact.offers[].note`).
- [ ] (선택) 팝업 크기/색/위치 미세 조정(`Contact.jsx`의 `Tally.openPopup` 옵션).
- [ ] (선택) 오퍼 문구(`ME.contact`) 다듬기.
- [ ] ⏸️ **보류(2026-08-19 — 스레드 스킬 삭제)** (선택) 다른 프로젝트 앵커 스레드 작성(짱샘의 책방·중개프로 등).

### 핵심 참조
- **데이터 한 곳**: `src/projects.js` → `ME.contact` (`formUrl`, `formId`, `email`, `offers[].typeValue` 등). 문구·오퍼 수정은 여기.
- **Tally 폼**: ID `b5EKOL` · 숨김필드 이름 **`type`**(코드의 `?type=`/hiddenFields와 짝) · 계정/알림 메일 `junominu@gmail.com`.
- **관련 문서**: `CLAUDE.md` → "코칭·외주 문의 (/contact)" 섹션.
- **배포**: `main` push → Vercel 자동 배포 → junominu.com.

<!-- 다음 세션 기록은 이 줄 위에, 최신이 위로 오도록 새 날짜 섹션으로 추가 -->
